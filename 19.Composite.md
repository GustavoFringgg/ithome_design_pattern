Composite 的核心概念

GoF 定義："Compose objects into tree structures to represent part-whole hierarchies. Composite lets clients treat individual objects and compositions of objects uniformly."

（把物件組合成樹狀結構，用來表示「部分-整體」的階層關係。Composite 讓客戶端可以用同一種方式對待單一物件跟一堆物件組成的群組）

核心角色

1. Component（共同介面）：leaf 跟 composite 都要實作的介面，定義一套「不管你是單一物件還是一堆物件，都該支援的操作」（例如：算價錢、顯示名稱）
2. Leaf（葉節點）：樹的最底層，沒有子節點，直接實作 Component 介面，回傳自己的值
3. Composite（組合節點）：本身也實作 Component 介面，但內部持有一個 List<Component>（可能是一堆 Leaf，也可能還包著別的 Composite）。它實作介面方法的方式通常是：對每一個子節點呼叫同一個方法，再把結果聚合起來（遞迴）
4. Client：只認識 Component 這個介面，不用判斷「我現在拿到的到底是單一物件還是一個群組」，呼叫方式完全一樣，遞迴的事交給 Composite 自己處理

為什麼會有 Composite——它解決的問題

如果沒有 Composite，你想操作一個「有階層的樹狀結構」（檔案系統的資料夾/檔案、UI 的容器/元件、組織圖的部門/員工），程式碼常常會長這樣：

if (這是一個單一物件) {
直接處理
} else if (這是一個群組) {
for each 子物件 {
遞迴呼叫自己 // 還要自己手動處理遞迴
}
}

每個要走訪這棵樹的地方，都得重新判斷型別、重新寫一次遞迴邏輯——判斷邏輯散落各處，新增一種節點型別，所有呼叫端都要跟著改

Composite 把「這是 leaf 還是 composite」這個判斷，收進樹狀結構自己內部：composite 知道怎麼把操作轉發給自己的子節點，leaf 知道怎麼直接回答。Client 端完全不用關心現在站在樹的哪一層，統一呼叫 Component 介面就好，遞迴這件事，是樹自己知道怎麼做，不是呼叫端每次要重新寫一次。

需要留意的取捨（先埋個伏筆，之後討論段可以展開）

- 優點：Client 程式碼大幅簡化，新增節點類型（新的 Leaf 或 Composite 變形）也不用改動 Client
- 代價：Component 介面如果設計得太寬，容易讓 Leaf 被迫實作一些對它不適用的方法（例如 Add(child)——leaf 根本沒有子節點可以加），這是 Composite 常見的爭議點：介面層級沒辦法限制「只有 composite 才能加子節點」，型別安全打了折扣
