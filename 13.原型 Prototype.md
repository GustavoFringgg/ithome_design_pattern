# Day 13｜Prototype——複製常客的慣用配方

今天來聊聊 Prototype 原型模式

我們來看看原文的定義，來自 Design Patterns: Elements of Reusable Object-Oriented Software

> Specify the kinds of objects to create using a prototypical instance, and create new objects by copying this prototype.

蝦咪意思，用中文理解可以是

> 先準備好一個「原型（Prototype）」實體當範本，之後要建立新物件時，不透過建構子重新跑一次建構流程，而是直接複製這個範本，再依需求做局部調整

核心特性

1. 用複製取代重建：不再從零呼叫建構子，而是拿現成的實體直接拷貝一份
2. 範本可以先客製化好：原型本身就是一個已經設定好各種細節的實例，複製出來的東西自帶這些細節
3. 複製後彼此獨立：複製出來的新物件，改動後不該影響到原本的範本，兩者要能各自變動

先舉個生活化的例子，找工作投履歷的時候，我們通常會先準備好一份「範本履歷」，姓名、學歷、大部分的技能都先填好

```csharp
public class Resume
{
    public string Name { get; set; }
    public string Title { get; set; }
    public int YearsOfExperience { get; set; }

    // 自定義 Clone()，回傳型別直接寫 Resume
    public Resume Clone()
    {
        return (Resume)this.MemberwiseClone(); // MemberwiseClone() 回傳的是 object，這裡轉型一次就好
    }
}
```

每次要投一間新公司，就複製這份範本，只改職稱這個欄位就好，其他不用重打一次

```csharp
var baseResume = new Resume { Name = "阿柴", Title = "全端工程師", YearsOfExperience = 1 };

var resumeForA = baseResume.Clone();
resumeForA.Title = "後端工程師"; // 投 A 公司時想掛的職稱

var resumeForB = baseResume.Clone();
resumeForB.Title = "前端工程師"; // 投 B 公司時想掛的職稱

Console.WriteLine(baseResume.Title);  // 全端工程師（範本沒被動到）
Console.WriteLine(resumeForA.Title);  // 後端工程師
Console.WriteLine(resumeForB.Title);  // 前端工程師
```

每份複製出來的履歷都是獨立的，改一份不會動到另一份

等等等，你是不是也會想到，改這麼麻煩，我不直接 new 三個實體就好了?

說的也是....(欸不是)，因為這是簡單的範例(我比較好懂)，但實際上來說，軟體結構是非常複雜且龐大的

並不是通通 new 就可以治百病，我們可以來初步判斷什麼時候不能／不想直接 new？

1. 建立成本太高（昂貴的初始化）
   如果 new Resume() 的建構子內部需要讀取硬碟設定檔、打 API 查資料庫、或是跑演算法計算（耗時 500ms），直接 new 3 次就要耗費 1.5 秒<br>
   用 Clone() 是直接在記憶體層級拷貝現有狀態，耗時幾乎是 0ms

2. 初始化狀態極其繁瑣（20+ 個欄位預設值）
   假設履歷有 30 個欄位，其中 28 個欄位（學歷、自傳、聯絡電話、專案經驗、排版設定...）在不同版本間都一模一樣，只有 2 個欄位要改<br>
   若直接 new，每開一個新實體就得手動重新 assign 28 次相同屬性，程式碼會充滿冗長且重複的賦值邏輯<br>
   用 Clone() 則是一行取得包含 28 個預設值的新物件，再改那 2 個即可。

3. 呼叫端不知道（也不該知道）具體類別與建構細節
   有時你的程式碼只拿到一個介面（例如 IResume template），你不知道它背後是哪一個具體的 Class，它可能是 FrontendResume、BackendResume 甚至 SpecialFormatResume，這時你根本無法寫 new ???()，但只要有 Prototype 介面，直接呼叫 template.Clone() 就能完美複製出正確型別的實體。

好，那我們一樣回到柴咖啡

## 回到柴咖啡

昨天用 Singleton 解決了咖啡機連線被搶著用的問題，阿柴這幾天觀察櫃檯，發現一件事

柴咖啡有一群常客，每次來都點差不多的配方：「大杯、少冰、半糖、燕麥拿鐵」，還會交代幾句備註，像是「不要吸管」

小黑跟阿柴抱怨：「這幾位熟客我都認得了，可是店員每次還是要重新問一輪『大杯少冰半糖燕麥奶對嗎？備註一樣嗎？』，客人自己都覺得煩，你能不能讓系統直接記得上次那杯，用複製的就好？」

## 先看看阿柴的寫法

系統裡本來就留著這位客人上一杯的 `Latte` 物件（用昨天的 `LatteBuilder` 組出來的），阿柴心想，把每個欄位手動讀出來、塞進新物件就好

```csharp
public class Latte
{
    public CupSize Size { get; set; }
    public IceLevel IceLevel { get; set; }
    public SugarLevel SugarLevel { get; set; }
    public MilkType MilkType { get; set; }
    public List<string> Notes { get; set; } = new(); // 備註，例如「不要吸管」
}
```

```csharp
var lastOrder = new Latte
{
    Size = CupSize.Large,
    IceLevel = IceLevel.Less,
    SugarLevel = SugarLevel.Half,
    MilkType = MilkType.Oat,
};
lastOrder.Notes.Add("不要吸管");

var newOrder = new Latte
{
    Size = lastOrder.Size,
    IceLevel = lastOrder.IceLevel,
    SugarLevel = lastOrder.SugarLevel,
    MilkType = lastOrder.MilkType,
};
foreach (var note in lastOrder.Notes)
{
    newOrder.Notes.Add(note); // 備註也要一條一條搬過去
}
```

問題解決了沒有錯，但以後 `Latte` 只要多一個欄位（比如加了會員編號、集點數），這段手動複製的程式碼就要跟著補一行，只會等到某天發現新訂單少了個欄位才知道漏寫

是不是跟 Day 11 疊一堆建構子的麻煩很像，是同一種「靠人工同步、容易漏東西」的重複勞動

## 這時候我們派 Prototype 上場

阿柴仿照剛剛履歷的做法，在 `Latte` 上自己定義一個 `Clone()`，可以不用一個個手動列欄位，直接產出一份複製品

```csharp
public class Latte
{
    public CupSize Size { get; set; }
    public IceLevel IceLevel { get; set; }
    public SugarLevel SugarLevel { get; set; }
    public MilkType MilkType { get; set; }
    public List<string> Notes { get; set; } = new();

    public Latte Clone()
    {
        return (Latte)this.MemberwiseClone(); // 逐一複製目前所有欄位
    }
}
```

呼叫端

```csharp
var lastOrder = new Latte
{
    Size = CupSize.Large,
    IceLevel = IceLevel.Less,
    SugarLevel = SugarLevel.Half,
    MilkType = MilkType.Oat,
};
lastOrder.Notes.Add("不要吸管");

var newOrder = lastOrder.Clone();
newOrder.Notes.Add("杯套加厚"); // 這次客人多交代一句

Console.WriteLine(string.Join(", ", lastOrder.Notes));
```

```text
不要吸管, 杯套加厚
```

阿柴發現，明明只在 `newOrder` 加了一句備註，`lastOrder`（那杯範本）的備註也一起多了一條

問題出在 `MemberwiseClone()` 只複製「欄位本身」：`Size`、`IceLevel` 這種 enum 是值型別，複製出來的是各自獨立的值，改一邊不會動到另一邊；但 `Notes` 是 `List<string>`，欄位裡存的其實只是「指向同一份清單的參照」，`MemberwiseClone()` 複製到的也只是這個參照，不是清單本身，所以 `newOrder.Notes` 跟 `lastOrder.Notes` 從頭到尾指向同一份清單，改一邊等於兩邊都改了

這就是**淺拷貝（Shallow Copy）**的陷阱：值型別複製沒問題，參照型別複製到的只是「同一份參照」，不是各自獨立的副本

## 換成深拷貝

要讓每一杯都是各自獨立的訂單，`Notes` 也得複製一份新的清單出來，而不是複製參照

```csharp
public Latte Clone()
{
    var copy = (Latte)this.MemberwiseClone(); // 值型別欄位先照舊複製一輪
    copy.Notes = new List<string>(this.Notes); // 參照型別另外複製一份新清單
    return copy;
}
```

再跑一次剛剛的情境

```csharp
var newOrder = lastOrder.Clone();
newOrder.Notes.Add("杯套加厚");

Console.WriteLine(string.Join(", ", lastOrder.Notes)); // 不要吸管
Console.WriteLine(string.Join(", ", newOrder.Notes));  // 不要吸管, 杯套加厚
```

這次兩杯的備註互不影響，`newOrder` 是從 `lastOrder` 複製出來的獨立副本，阿柴接下來只要微調有變動的欄位就好，其他跟上次一樣的部分完全不用重打

```csharp
var newOrder = lastOrder.Clone();
newOrder.Size = CupSize.Medium; // 這次只想換成中杯，其他都跟上次一樣
```

## Prototype 的取捨

**先判斷欄位是值型別還是參照型別，才知道淺拷貝夠不夠**：目前 `Latte` 的 `Size`、`IceLevel`、`SugarLevel`、`MilkType` 都是 enum（值型別），淺拷貝直接複製就沒問題；但只要有一個參照型別欄位（`List`、另一個 class 的實例），就得針對那個欄位手動處理，不然會共享同一份內部資料

**深拷貝不用對所有欄位都下手**：如果每加一個欄位就把整個物件都寫成手動深拷貝，維護成本只會往上堆；只對「真的是參照型別、真的需要各自獨立」的欄位處理就好，值型別欄位交給 `MemberwiseClone()` 就夠了

**建構成本夠低時，不一定要上 Prototype**：目前 `Latte` 本身建構很單純，就是幾個 enum 欄位塞進去，就算不用 `Clone()`，直接用昨天的 `LatteBuilder` 重新組一杯也不貴。阿柴會想用 Prototype，主要是想省掉「重新問一次客製化選項」這個溝通成本，不是因為建構本身很貴

## 邊界在哪

Prototype 真正划算的場景，是「重新建立一個物件的成本，比複製一份現有的貴很多」的時候。舉例來說，如果柴咖啡未來串了會員系統，組一杯飲料前還得先呼叫 API 查會員等級、優惠資格，每次重新建構都要打一次 API，這時候複製一份「上次組好的物件」再改欄位，會比每次都重新呼叫 API 划算很多

但如果只是像現在的 `Latte` 這樣，欄位單純、建構又快，Prototype 省下來的與其說是效能，不如說是「不用重新問一次客製化選項」這個體驗上的方便，用不用就看阿柴覺得多維護一份 `Clone()` 邏輯划不划算

深拷貝也有自己的邊界：如果物件裡有巢狀物件、甚至物件之間互相參照（A 參照 B、B 又參照 A 這種循環參照），手寫遞迴深拷貝要小心別寫成無窮迴圈，這時候有些人會改用序列化再反序列化的方式來做深拷貝，但也要注意序列化碰不到的東西（像私有欄位、事件綁定）可能會漏掉，不是萬靈丹

所以動手寫 `Clone()` 前，可以先問自己：**這個物件裡，哪些欄位是值型別可以放心共用複製結果，哪些欄位是參照型別、一旦共用就會互相污染？**

回到這系列的核心主軸，如果沒把這件事想清楚就叫 AI 寫 `Clone()`，常見會看到兩種產出：一種是直接叫 AI 寫 `MemberwiseClone()` 就交差，遇到 `List` 或物件參照欄位就埋雷，要等到兩筆資料開始互相污染才被發現；另一種是保守地叫 AI 對整個物件所有欄位都手寫遞迴深拷貝，即使多數欄位只是 enum、int 這種值型別，也平白多了一堆沒必要的複製邏輯與維護成本。這兩種寫法 AI 都寫得出語法正確的 `Clone()`，但它沒辦法自己判斷「這個欄位該共用還是該獨立」，只有把物件的欄位組成、哪些該共享、哪些該獨立想清楚講給 AI 聽，才能讓它把 `Clone()` 寫在刀口上

---

明天把創建型五個 Pattern（Factory Method、Abstract Factory、Builder、Singleton、Prototype）攤開來做個總結對比，看看它們各自解決什麼問題、又是怎麼分工的

## 參考資料

[Refactoring Guru - Prototype](https://refactoring.guru/design-patterns/prototype)
