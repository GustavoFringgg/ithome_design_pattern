# Day 12｜Singleton——咖啡機狀態,全店共用一份

今天來聊聊 Singleton Pattern 單例模式

我們來看看原文的定義，來自 Design Patterns: Elements of Reusable Object-Oriented Software

> "Ensure a class only has one instance, and provide a global point of access to it."

蝦咪意思，用中文理解可以是

> 確保一個類別（Class）在整個應用程式生命週期中，永遠只有一個實例（Instance），並提供一個全域訪問點來取得該實例

核心特性

1. 唯一性：限制外部不能隨意 new 出多個實例

2. 自我管理：類別自己負責建立、快取並管理這唯一的一個實例

3. 全域存取：提供統一的方法（通常是靜態方法或模組匯出）讓外部呼叫

有些朋友可能會覺得熟悉，像網站開發時就會很常遇到，舉例來說

1. 資料庫連線池（Database Connection Pool）：避免重複建立昂貴的連線

2. 日誌紀錄器（Logger）：確保所有模組寫入同一個 Log 物件與檔案流

3. 應用程式全域設定（App Config）：全域共享同一個讀取完畢的設定檔

4. 快取管理（Cache Manager）：統一管理記憶體快取

好接下來，又到了我們快樂舉例的時間了，想當然身為一個專業的社畜，一定要會使用印表機，每個辦公室也一定會有一台

這裡我們使用 C# 原生型別 Lazy<T> 去舉例

```csharp
using System; // 要引入才能使用 Lazy<T>

// 1. sealed：防止其他類別繼承它而破壞單例
public sealed class OfficePrinter
{
    // 2. 使用 Lazy<T>：負責「建立物件時」的執行緒安全與延遲載入，也只有在第一次有人要印東西時，才真正連線開機
    private static readonly Lazy<OfficePrinter> _lazyInstance = new Lazy<OfficePrinter>(() => new OfficePrinter());

    // 3. 私有建構函式：防止外部隨意 new OfficePrinter()
    private OfficePrinter()
    {
        Console.WriteLine("--> [硬體初始化] 正在連線至實體印表機 IP、載入驅動...（只會執行這一次）");
    }

    // 4. 全域唯一的存取點
    public static OfficePrinter Instance => _lazyInstance.Value;

    // 業務方法：大家共用同一個佇列印文件
    public void PrintDocument(string employeeName, string documentName)
    {
        Console.WriteLine($"[列印中] 員工 {employeeName} 的文件：{documentName}");
    }
}
```

那怎麼呼叫跟驗證呢

```csharp
class Program
{
    static void Main(string[] args)
    {
        Console.WriteLine("=== 辦公室系統啟動 ===");
        // 此時印表機尚未連線初始化

        Console.WriteLine("\n員工小明準備列印...");
        // 第一次呼叫 Instance：觸發 Lazy 初始化
        var printerA = OfficePrinter.Instance;
        printerA.PrintDocument("小明", "履歷表.pdf");

        Console.WriteLine("\n員工小美準備列印...");
        // 第二次呼叫 Instance：直接使用已建立好的實例
        var printerB = OfficePrinter.Instance;
        printerB.PrintDocument("小美", "財務報表.xlsx");

        // 驗證兩者使用的是同一台機器
        Console.WriteLine($"\n小明和小美使用的是同一台印表機嗎？ {ReferenceEquals(printerA, printerB)}");
    }
}
```

接下來我們來看結果

```text
=== 辦公室系統啟動 ===

員工小明準備列印...
--> [硬體初始化] 正在連線至實體印表機 IP、載入驅動...（只會執行這一次）
[列印中] 員工 小明 的文件：履歷表.pdf

員工小美準備列印...
[列印中] 員工 小美 的文件：財務報表.xlsx

小明和小美使用的是同一台印表機嗎？ True
```

是不是跟你想的一樣?，很棒，我們再來回顧一下定義

> 確保「一個」類別，在整個應用程式生命週期中，永遠只有「一個」實例，並提供「一個」全域訪問點來取得該實例

那可能有人跟我有一樣的疑問?<br>
那一定要有 Lazy<T> 才能實踐 Singleton 嗎? 不是，Singleton 的核心只有兩件事

1. 全域只有一個實例
2. 提供全域存取點

雖然可以不使用 Lazy<T> 也可以達成 Singleton，但我擔心複雜度會直線上升(畢竟我是菜鳥...)

但 Lazy<T> 可以將延遲載與多執行緒安全兩大複雜需求封裝成一個原生型別，讓工程師把精力聚焦在業務邏輯上，而非底層鎖的競爭控制上

所以後續我們都用 Lazy<T> 來實踐吧

## 回到柴咖啡

昨天用 Builder 解決了「一杯飲料的客製化選項」，阿柴喘口氣，覺得點餐這條線總算穩了

這次小黑丟來的問題，是那台實體咖啡機：最近常有客人反應咖啡忽濃忽淡，甚至有一次師傅正在保養清潔機器，前台居然還送了一杯單子進去出餐

小黑覺得奇怪，機器保養的時候，系統不是應該知道要暫停接單嗎，怎麼會還送單進去

## 先看看小黃留下的程式碼出了什麼問題

阿柴翻開小黃的舊程式碼，發現點餐流程跟保養流程，各自都用一個 `CoffeeMachine` 類別在記錄機器狀態，而且各自 `new` 了一份

```csharp
public class CoffeeMachine
{
    public bool IsUnderMaintenance { get; private set; }

    public void EnterMaintenance() => IsUnderMaintenance = true;
    public void ExitMaintenance() => IsUnderMaintenance = false;

    public void Brew(string drinkName)
    {
        Console.WriteLine($"正在萃取：{drinkName}");
    }
}
```

```csharp
// 保養面板：師傅按下開始保養
public class MaintenancePanel
{
    private CoffeeMachine _machine = new CoffeeMachine();

    public void StartCleaning()
    {
        _machine.EnterMaintenance();
        Console.WriteLine("機器進入保養模式，暫停出餐");
    }
}

// 點餐區：客人點餐時要先確認機器狀態
public class OrderCounter
{
    private CoffeeMachine _machine = new CoffeeMachine();

    public void TakeOrder(string drinkName)
    {
        if (_machine.IsUnderMaintenance)
        {
            Console.WriteLine("機器保養中，暫停接單");
            return;
        }
        _machine.Brew(drinkName);
    }
}
```

師傅在 `MaintenancePanel` 按下開始保養，那份 `CoffeeMachine` 的 `IsUnderMaintenance` 確實變成了 `true`

但 `OrderCounter` 手上是另一份完全獨立的 `CoffeeMachine`，那份的 `IsUnderMaintenance` 還停在初始值 `false`

```csharp
var maintenance = new MaintenancePanel();
maintenance.StartCleaning(); // 機器進入保養模式，暫停出餐

var counter = new OrderCounter();
counter.TakeOrder("美式"); // 正在萃取：美式  ← 明明機器正在保養
```

語法完全正確，編譯器不會抓到任何錯誤，但點餐區完全不知道機器正在保養，還是把單子送了進去，因為兩邊根本不是同一份物件在管理同一台實體機器

問題不在「物件怎麼生」，而在**明明只有一台實體機器，卻被 `new` 成了兩份各自獨立、互不知情的軟體物件**

## Singleton 上場

Singleton(單例模式)要解決的問題很純粹

> 確保一個類別只有一個實體，並提供一個全域存取點

拆開來看，其實就兩件事

1. **只有一個實體**：不管在程式裡的哪個角落想拿 `MenuConfig`，拿到的永遠是同一份記憶體位置上的物件
2. **全域存取點**：不用把這個物件層層傳遞注入到每個需要它的地方，直接透過一個固定的入口就能拿到

作法上，把建構子鎖起來(private)，不讓外部隨便 `new`，改成透過一個靜態屬性統一取用

```csharp
public class MenuConfig
{
    // 1. 用 Lazy<T> 包一層，確保「第一次有人真的用到」時才建立，且天生執行緒安全
    private static readonly Lazy<MenuConfig> _instance = new(() => new MenuConfig());

    // 2. 唯一的全域存取點
    public static MenuConfig Instance => _instance.Value;

    private Dictionary<string, bool> _availability = new();

    // 3. 建構子上鎖，外部沒辦法自己 new MenuConfig()
    private MenuConfig()
    {
        _availability["美式"] = true;
        _availability["拿鐵"] = true;
        _availability["摩卡"] = true;
    }

    public void SetSoldOut(string drinkName) => _availability[drinkName] = false;

    public bool IsAvailable(string drinkName) => _availability.GetValueOrDefault(drinkName, true);
}
```

阿柴把 `AdminPanel`、`OrderCounter` 也跟著改掉，兩邊不再各自 `new`，全部改成向同一個入口拿

```csharp
public class AdminPanel
{
    public void MarkSoldOut(string drinkName)
    {
        MenuConfig.Instance.SetSoldOut(drinkName); // 拿的是同一份
        Console.WriteLine($"{drinkName} 已標記為售完");
    }
}

public class OrderCounter
{
    public void TakeOrder(string drinkName)
    {
        if (MenuConfig.Instance.IsAvailable(drinkName)) // 還是同一份
            Console.WriteLine($"幫您點一杯 {drinkName}");
        else
            Console.WriteLine($"不好意思，{drinkName} 賣完了");
    }
}
```

再跑一次剛剛的情境

```csharp
var admin = new AdminPanel();
admin.MarkSoldOut("美式");

var counter = new OrderCounter();
counter.TakeOrder("美式"); // 不好意思，美式 賣完了 ← 這次終於對上了
```

因為 `MenuConfig.Instance` 從頭到尾都是同一顆物件，`AdminPanel` 改的狀態，`OrderCounter` 讀到的就是改過的那份，不會再有兩邊各自為政的情況

> 為什麼用 `Lazy<T>`，不直接 `private static MenuConfig _instance = new MenuConfig();`？<br>
> 後者叫「餓漢式」，程式一啟動就會建立實體，簡單但沒辦法延後初始化；`Lazy<T>` 叫「懶漢式」，等真正第一次用到才建立，且 .NET 保證 `Lazy<T>` 在多執行緒情境下只會初始化一次，不用自己手刻 lock

## Singleton 的取捨

Singleton 看起來乾淨俐落，但阿柴很快就發現代價

**變成隱藏依賴**：`OrderCounter` 內部直接寫死 `MenuConfig.Instance`，從建構子完全看不出這個類別依賴了 `MenuConfig`，這正好違反 Day 7 講的依賴反轉——依賴關係應該要顯式(透過建構子或介面)講清楚，而不是藏在方法內部偷偷取用全域變數

**難以測試**：如果阿柴想幫 `OrderCounter` 寫單元測試，測試「美式賣完時應該拒絕點餐」，會發現沒辦法輕易換一份假的 `MenuConfig` 進去，因為 `MenuConfig.Instance` 是寫死的，每個測試案例還會共用同一份全域狀態，測試案例之間互相汙染

**全域可變狀態本身就是風險源**：任何地方都能呼叫 `MenuConfig.Instance.SetSoldOut(...)`，改動可以從程式的任何角落發生，出問題時很難追出到底是誰改的

## 邊界在哪

**「只有一份」是相對於目前的規模，不是絕對真理**

現在的柴咖啡只有一間店，菜單設定、庫存全店共用一份是合理的假設。但如果哪天真的展成連鎖店(這系列後面會走到那一步)，每家分店的庫存其實不該共用同一份——A 店的牛奶用完，不代表 B 店也要跟著顯示售完

到那個時候，`MenuConfig.Instance` 這種寫死成「整個程式只有一個」的全域單例就會變成阻礙，因為它的假設從一開始就是「全世界只有一份」，而不是「每個分店各一份」。要撐住多分店，得改成用依賴注入去管理「每個分店一個實體」的生命週期(例如注入容器裡設定成 per-store 的 Scoped 生命週期)，而不是繼續用 `static` 硬幹

所以動手寫 Singleton 前，先問自己：**這份狀態，是「全域只該有一份」，還是「目前湊巧只有一份」？** 前者才適合 Singleton，後者只是現況剛好如此，用了 Singleton 反而會在規模變化時卡住

## AI 產出程式碼時，Singleton 能拿來檢查什麼

如果阿柴把「幫我寫一個全店共用的菜單設定」丟給 AI，同樣容易看到兩種極端

保守版本，AI 可能完全不管「共用」這個需求，就寫出一個普通類別，讓呼叫端自己到處 `new`，於是又回到阿柴一開始踩到的那個坑——各自一份、狀態對不上，因為 AI 沒有被告知「這份資料本來就該全域唯一」

過度發揮版本，AI 則常常見到什麼都想包成 Singleton：`OrderCounter`、`Cashier`、`KitchenService` 全部都被包成 `.Instance`，理由只是「反正比較好拿、少寫一次注入」。但這些類別本身沒有「只能有一份」的業務理由，純粹是圖方便把 Singleton 當成免注入的全域變數在用，結果是整包程式碼互相用 `.Instance` 勾在一起，日後要拆分、要測試、要因應展店切成多份實體，全部都要重新來過

AI 給得出 Singleton 該長什麼樣子的語法，甚至能一次生出 `Lazy<T>`、private 建構子這些細節都不出錯，但它答不出「柴咖啡裡，到底哪些資料是真的全域唯一，哪些只是暫時看起來只有一份」——這件事得靠人先把「唯一性」的邊界劃清楚，講給 AI 聽，AI 產出的 Singleton 才會用在真正該用的地方，而不是變成到處都是的全域變數

---

明天來聊聊柴咖啡的常客，同一位客人常常點同一款客製化配方，每次都要重新交代一次「半糖、少冰、燕麥奶」有點麻煩，該怎麼把常客的配方「複製」一份出來直接用，這時候輪到 **Prototype** 上場

## 參考資料

[Refactoring Guru - Singleton](https://refactoring.guru/design-patterns/singleton)
