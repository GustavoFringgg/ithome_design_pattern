# Day 16｜Decorator——飲料加購(加濃縮、加珍珠、加奶蓋)

昨天用 Adapter 把外送平台的資料格式接進柴咖啡的系統，今天繼續留在結構型 Pattern

來聊聊 Decorator 裝飾器模式

原文的定義，一樣來自 Design Patterns: Elements of Reusable Object-Oriented Software

> Attach additional responsibilities to an object dynamically. Decorators provide a flexible alternative to subclassing for extending functionality.

用中文理解可以是

> 動態地為一個物件附加額外的職責，裝飾器提供了比繼承更有彈性的方式，來擴充物件的功能

核心特性

1. 不改動原本的物件：想加功能，不用回頭改原本的類別，也不用開一個子類別去繼承
2. 一層包一層：裝飾器把原本的物件包起來，自己也實作跟原物件相同的介面，呼叫端分不出來現在拿到的是本尊還是被包過的版本
3. 可以疊加、可以任意順序疊加：想加幾層就加幾層，想先加 A 再加 B，或先加 B 再加 A，結果都能成立

先舉個生活化的例子，包禮物

一份禮物本身就能送人，但想讓它更體面，可以先包上包裝紙，也可以再綁上緞帶，包裝紙、緞帶都不會動到禮物本身，只是在外面多包一層，而且想加幾層都行

```csharp
// 呼叫端在乎的介面：能描述自己、能算出價錢
public interface IGift
{
    string Describe();
    int Price { get; }
}

// 本體：一份最單純的禮物
public class BasicGift : IGift
{
    public string Describe() => "一份禮物";
    public int Price => 200;
}

// 裝飾器基底：長得跟本體一樣，但內部包著另一個 IGift
public abstract class GiftDecorator : IGift
{
    protected readonly IGift _gift;

    protected GiftDecorator(IGift gift)
    {
        _gift = gift;
    }

    public abstract string Describe();
    public abstract int Price { get; }
}

// 具體裝飾器：各自只負責「加一件事」，其他都轉呼叫被包住的物件
public class WrappingPaperDecorator : GiftDecorator
{
    public WrappingPaperDecorator(IGift gift) : base(gift) { }

    public override string Describe() => $"{_gift.Describe()}，包上包裝紙";
    public override int Price => _gift.Price + 30;
}

public class RibbonDecorator : GiftDecorator
{
    public RibbonDecorator(IGift gift) : base(gift) { }

    public override string Describe() => $"{_gift.Describe()}，綁上緞帶";
    public override int Price => _gift.Price + 20;
}
```

呼叫端這樣用

```csharp
IGift gift = new RibbonDecorator(new WrappingPaperDecorator(new BasicGift()));

Console.WriteLine($"{gift.Describe()}，{gift.Price} 元");
// 一份禮物，包上包裝紙，綁上緞帶，250 元
```

`BasicGift` 從頭到尾沒被改過，`WrappingPaperDecorator`、`RibbonDecorator` 也各自只知道自己要加什麼，包了幾層、包的順序，都是呼叫端當下決定的

## 回到柴咖啡

外送平台接上線後，小黑又有新點子：飲料開放加購，加濃縮 +20、加珍珠 +15、加奶蓋 +20，客人可以自由搭配，也可以同一種加購項目重複加（例如「雙倍濃縮」）

阿柴翻開小黃留下的產品類別，也就是 Day 9 用過的那組

```csharp
public interface IDrink
{
    string Name { get; }
    int Price { get; }
}

public class Latte : IDrink
{
    public string Name => "拿鐵";
    public int Price => 90;
}

public class Americano : IDrink
{
    public string Name => "美式";
    public int Price => 70;
}

public class Mocha : IDrink
{
    public string Name => "摩卡";
    public int Price => 100;
}
```

現在客人要點的不再是單純一杯拿鐵，而是「拿鐵，加濃縮，加珍珠」這種原本的飲料加上任意組合的加購項目

## 先看看阿柴趕工出來的版本

離上線時間很趕，阿柴先求有，客人點什麼組合，就照著開一個對應的類別

```csharp
public class LatteWithExtraShot : IDrink
{
    public string Name => "拿鐵 + 加濃縮";
    public int Price => 90 + 20;
}

public class LatteWithBoba : IDrink
{
    public string Name => "拿鐵 + 加珍珠";
    public int Price => 90 + 15;
}

public class LatteWithExtraShotAndBoba : IDrink
{
    public string Name => "拿鐵 + 加濃縮 + 加珍珠";
    public int Price => 90 + 20 + 15;
}

public class AmericanoWithExtraShot : IDrink
{
    public string Name => "美式 + 加濃縮";
    public int Price => 70 + 20;
}

// 摩卡、加奶蓋、加濃縮加奶蓋、加珍珠加奶蓋...後面還有一堆
```

上線前勉強能動，但阿柴自己數了一下，4 種飲料 × 3 種加購項目，光是「每種加購選加或不加」的組合就有 4 × 2³ = 32 種，這還沒算進「加濃縮可以疊到兩份、三份」這種同一項目重複加購的情況

某天小黑把加濃縮從 +20 調成 +25，阿柴才發現這個金額被複製貼上寫死在十幾個類別裡，改一個要記得改十幾個地方，漏改一個，那杯飲料就用舊價錢賣出去了；下個月要是再加「加奶蓋」以外的第四種加購，類別數量會直接乘上去，翻倍成長

問題的根源不是阿柴手腳不夠快，而是**用「開子類別」來處理「可以自由疊加的加購」，組合數會呈指數成長，而且每一項加購的定價邏輯，被拆得到處都是**

## Decorator 上場

阿柴先確認呼叫端真正在乎的介面，其實一直都只有 `IDrink` 的 `Name` 跟 `Price`，不用改

```csharp
public interface IDrink
{
    string Name { get; }
    int Price { get; }
}
```

接著加一層裝飾器基底，長得跟 `IDrink` 一樣，但內部包著另一個 `IDrink`

```csharp
public abstract class DrinkDecorator : IDrink
{
    protected readonly IDrink _drink;

    protected DrinkDecorator(IDrink drink)
    {
        _drink = drink;
    }

    public abstract string Name { get; }
    public abstract int Price { get; }
}
```

每一種加購，各自寫一個裝飾器，只管「自己加了什麼」，其他一律轉呼叫被包住的飲料

```csharp
public class ExtraShotDecorator : DrinkDecorator
{
    public ExtraShotDecorator(IDrink drink) : base(drink) { }

    public override string Name => $"{_drink.Name} + 加濃縮";
    public override int Price => _drink.Price + 20;
}

public class BobaDecorator : DrinkDecorator
{
    public BobaDecorator(IDrink drink) : base(drink) { }

    public override string Name => $"{_drink.Name} + 加珍珠";
    public override int Price => _drink.Price + 15;
}

public class CreamCapDecorator : DrinkDecorator
{
    public CreamCapDecorator(IDrink drink) : base(drink) { }

    public override string Name => $"{_drink.Name} + 加奶蓋";
    public override int Price => _drink.Price + 20;
}
```

呼叫端這樣用，要包幾層、包哪幾種，當下依客人的點單決定

```csharp
// 拿鐵，加濃縮，加珍珠
IDrink order1 = new BobaDecorator(new ExtraShotDecorator(new Latte()));
Console.WriteLine($"{order1.Name}，{order1.Price} 元");
// 拿鐵 + 加濃縮 + 加珍珠，125 元

// 美式，加濃縮，加濃縮，加奶蓋（濃縮加兩份）
IDrink order2 = new CreamCapDecorator(new ExtraShotDecorator(new ExtraShotDecorator(new Americano())));
Console.WriteLine($"{order2.Name}，{order2.Price} 元");
// 美式 + 加濃縮 + 加濃縮 + 加奶蓋，130 元
```

原本 32 種以上的組合類別，現在只需要 4 個飲料類別 + 3 個裝飾器類別，全部收斂成 7 個；小黑要把加濃縮從 +20 調成 +25，阿柴只要動 `ExtraShotDecorator` 這一個地方，不用滿場找哪些組合類別忘了改

## Decorator 的取捨

**每個裝飾器只管自己加的那一件事**：`ExtraShotDecorator` 不該知道自己包的到底是拿鐵還是美式，也不該去管有沒有同時被包了 `BobaDecorator`，它只負責「在收到的 `Name`、`Price` 上面，加自己那一份」，其他一律轉呼叫

**用組合，不是繼承**：裝飾器是在建構子把原本的物件包進來（組合），不是去繼承 `Latte` 再開一個 `LatteWithExtraShot` 子類別（繼承）——這正是 Decorator 存在的理由，把「繼承會爆炸的組合數」換成「組合可以疊加的彈性」

**跟 Builder（Day 11）處理的層次不一樣**：Builder 是「把一個物件內部的欄位一步步組出來」，組完 `Build()` 之後形狀就固定了；Decorator 是「在一個已經存在的物件外面一層層加行為」，可以在點單當下依客人臨時加購動態疊加，甚至同一種加購可以重複疊好幾層，這是 Builder 天生做不到的

## 邊界在哪？什麼時候不需要 Decorator

如果柴咖啡的加購項目就只有一種，而且是「加或不加」的二選一，不會疊加、也不太會再增加新的加購項目，直接在 `IDrink` 上開一個 `bool HasExtraShot` 屬性，或是回頭用 Day 11 的 `LatteBuilder` 多加一個 `AddExtraShot()` 就夠了，不需要為此建一整套 `DrinkDecorator` 階層

Decorator 真正該出手的時機，是**加購項目數量會持續增加、彼此可以任意組合、甚至同一項目可以重複疊加**——柴咖啡現在的加購需求剛好符合這三個條件，才值得多這一層抽象

## AI 判斷力：這篇學到的判斷條件，能拿來檢查 AI 產出什麼

如果阿柴把「幫我加飲料加購功能，加濃縮、加珍珠、加奶蓋」丟給 AI，常見會看到兩種產出

一種是 AI 直接照著需求，一個組合開一個類別，或是在 `Latte`、`Americano` 上面各自開一排 `bool` 屬性，`Price` 用一長串 if 判斷組合出最終金額——能動，但這正是阿柴踩過的那個坑，加購項目一多、組合一多，就是指數成長的類別數，或是散落各處改不完的定價邏輯

另一種是 AI 直接生出一整套可動態註冊的加購規則引擎，支援任意條件式定價、加購項目彼此互斥設定、甚至留了後台可以自訂加購邏輯的 DSL——如果柴咖啡現在就只有三種固定加價的加購項目，這種規格已經是設計過頭，多出來的彈性根本用不到

AI 給得出「Decorator 該長什麼樣子」的語法，但它不知道柴咖啡現在到底有幾種加購項目、金額是不是簡單固定加價、會不會疊加、以後多久會再加一種新項目——這些屬於「這間店的加購規則現在長怎樣」的判斷，答案不在程式碼裡，只有阿柴自己清楚

把這些條件講清楚告訴 AI：「目前三種加購都是固定加價，同一項目可以重複疊加，之後可能還會再加一兩種」，AI 給出的 Decorator 層才會剛好貼合柴咖啡現在的規模，而不是幫一套還沒出現的複雜定價規則預先搭好一整個引擎

---

明天來聊柴咖啡的結帳流程，收銀、庫存、發票、通知，一次要串好幾個子系統，這種「把複雜流程包成一個簡單入口」的情境，輪到 **Facade** 外觀模式上場

## 參考資料

[Refactoring Guru - Decorator](https://refactoring.guru/design-patterns/decorator)

[[ Day 13 ] 你今天要來一份洋蔥圈嗎? - 裝飾者模式 ( Decorator Pattern )](https://ithelp.ithome.com.tw/articles/10206110)
