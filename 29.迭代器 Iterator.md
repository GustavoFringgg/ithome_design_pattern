# Day 29｜Iterator——不用管菜單樹長什麼樣，也能走完整份菜單

昨天用 Mediator 把廚房、收銀、外送平台之間的協調邏輯收進一個中介者，今天是這個系列最後一個新 Pattern：Iterator 迭代器模式

原文的定義，一樣來自 Design Patterns: Elements of Reusable Object-Oriented Software

> Provide a way to access the elements of an aggregate object sequentially without exposing its underlying representation.

用中文理解可以是

> 提供一種方法，可以循序走訪一個聚合物件內部的元素，且不需要暴露這個物件內部是怎麼儲存資料的

核心角色

1. Iterator（迭代器介面）：定義走訪用的方法，通常是「有沒有下一個」「拿目前這一個」（C# 是 `IEnumerator<T>` 的 `MoveNext()`／`Current`）
2. ConcreteIterator（具體迭代器）：真正記錄「現在走到哪裡了」的實作，知道怎麼從目前位置移到下一個
3. Aggregate（聚合介面）：定義一個「給我一個迭代器」的方法（C# 是 `IEnumerable<T>` 的 `GetEnumerator()`）
4. ConcreteAggregate（具體聚合物件）：實際持有一堆元素的集合本身

舉個生活化的例子

書局的書架，書可能照出版社排、可能照類別排、也可能是新書平台隨便疊。你想做的是「一本一本看下去」，不會管書架背後的收納邏輯是什麼

```csharp
// 1. Iterator（共同介面）：不管背後是怎麼收納的，都用同一種方式問「有沒有下一本」「現在這本是誰」
public interface IBookIterator
{
    bool HasNext();
    Book Next();
}

// 2. ConcreteIterator：知道自己現在走到書架的第幾格
public class BookShelfIterator : IBookIterator
{
    private readonly List<Book> _books;
    private int _index = 0;

    public BookShelfIterator(List<Book> books) => _books = books;

    public bool HasNext() => _index < _books.Count;
    public Book Next() => _books[_index++];
}
```

呼叫端只認識 `IBookIterator`，不用知道書架內部是 `List`、`Array`，還是別的資料結構

```csharp
IBookIterator iterator = bookShelf.CreateIterator();
while (iterator.HasNext())
{
    Console.WriteLine(iterator.Next().Title);
}
```

### 回到柴咖啡

Day 19 用 Composite 把菜單組成一棵樹：`柴咖啡菜單` 底下是 `飲料`／`點心`，`飲料` 底下又分 `熱的`／`冰的`，一路疊下去，`MenuCategory` 內部用 `_children: List<IMenuComponent>` 收著這些子節點，而且是 `private`，外面完全碰不到

現在小黑想串一個電子發票／記帳系統，對方要的資料格式很單純：不分類、不分層，就是「這間店現在賣的每一個品項」攤平成一份清單

阿柴心想，那就在 `MenuCategory` 上開一個新方法

```csharp
public class MenuCategory : IMenuComponent
{
    // ...既有的 _name、_children、Add()、Display() 不變

    public List<MenuItem> GetAllItems()
    {
        var result = new List<MenuItem>();
        foreach (var child in _children)
        {
            if (child is MenuItem item)
            {
                result.Add(item);
            }
            else if (child is MenuCategory category)
            {
                result.AddRange(category.GetAllItems()); // 遞迴收集
            }
        }
        return result;
    }
}
```

上線沒多久，行銷想找出「所有 100 元以下的品項」做促銷活動，阿柴又在 `MenuCategory` 開一個 `FindCheapItems(int maxPrice)`，內部一樣寫一次遞迴走訪；後來小黑想知道「菜單現在總共有幾項品項」，阿柴又補一個 `CountItems()`，內部還是同一套遞迴

三個方法要做的事完全不一樣，但走訪這棵樹的邏輯幾乎一模一樣，全部複製貼上重寫一次。以後每多一種「想對整份菜單做點什麼」的需求，就要回頭替 `MenuCategory` 再開一個新方法

### Iterator 上場

阿柴發現，真正該抽出來的不是某一種特定用途的方法，而是「怎麼走訪這棵樹」這件事本身。讓 `MenuCategory` 提供一個迭代器，走訪的過程要拿去做什麼，是呼叫端自己的事，不用 `MenuCategory` 每次都幫忙做好

C# 已經把 Iterator 這個角色定義成語言內建的兩個介面：`IEnumerator<T>`（迭代器）、`IEnumerable<T>`（聚合物件），還提供 `yield return` 讓實作變得很簡短，不用像剛剛書架的例子一樣自己維護一個索引游標

```csharp
public class MenuCategory : IMenuComponent, IEnumerable<MenuItem>
{
    // ...既有的 _name、_children、Add()、Display() 不變

    public IEnumerator<MenuItem> GetEnumerator()
    {
        foreach (var child in _children)
        {
            if (child is MenuItem item)
            {
                yield return item;
            }
            else if (child is MenuCategory category)
            {
                foreach (var nested in category) // 遞迴走訪子分類自己的迭代器
                {
                    yield return nested;
                }
            }
        }
    }

    IEnumerator IEnumerable.GetEnumerator() => GetEnumerator();
}
```

`MenuItem` 原本只為了 `Display()` 留了 private 欄位，現在要讓外部走訪、篩選，補上兩個唯讀屬性

```csharp
public class MenuItem : IMenuComponent
{
    private readonly string _name;
    private readonly int _price;

    public string Name => _name;
    public int Price => _price;

    // 建構子、Display() 不變
}
```

`GetAllItems()`、`FindCheapItems()`、`CountItems()` 這三個方法可以整組刪掉，呼叫端改成

```csharp
foreach (var item in menu) // MenuCategory 本身就能被 foreach，因為它是 IEnumerable<MenuItem>
{
    Console.WriteLine(item.Name);
}

int totalCount = menu.Count();                                   // 算總數
var cheapItems = menu.Where(item => item.Price < 100).ToList();  // 找便宜品項
```

`MenuCategory` 完全不知道呼叫端到底要拿這份走訪結果去算總數、篩選，還是丟給發票系統，它只負責回答「怎麼走訪」這一件事

### 其實你每天都在用

剛剛手動實作的 `IBookIterator`／`BookShelfIterator`，跟 `MenuCategory` 實作的 `GetEnumerator()`，做的其實是同一件事，只是後者接的是 C# 語言本身認得的介面

一旦一個類別實作了 `IEnumerable<T>`，`foreach` 背後呼叫的就是這個 `GetEnumerator()`，LINQ 的 `Count()`、`Where()`、`Select()` 這些方法，也全部建立在同一組介面上。這也是為什麼日常寫 C# 幾乎不會自己手刻一個 Iterator，語言早就把這個 Pattern 內建進基礎設施了；真的會自己實作的時機，通常只剩下「我有一個自訂的資料結構（像今天的樹），想讓它也能被 `foreach`、被 LINQ 直接使用」

## Iterator 的取捨

**資料結構穩定、也不太需要對外開放走訪時，不一定划算**：如果一個集合只有內部自己會用、不會被外部走訪，或走訪需求就只有一種、以後也不太會再增加，直接寫一個 `List<T>` 搭配內部私有方法，會比另外刻一組 Iterator 角色更省事

**走訪中資料被修改，是常見的坑**：像今天用 `yield return` 實作的走訪，如果在 `foreach` 跑到一半時，`_children` 被新增或刪除了一個節點，執行期通常會直接丟例外（.NET 是 `InvalidOperationException`）。這不是 Iterator 這個 Pattern 的缺陷，而是走訪跟修改同時發生時本來就該小心處理的情境——真的需要邊走訪邊修改，得考慮先複製一份快照，或改用允許安全修改的走訪方式

## 今天學到的事

Iterator 要解決的問題：

`需要走訪一個聚合物件內部的所有元素，但不想讓呼叫端知道這個聚合物件內部是怎麼儲存資料的（陣列、清單、樹狀結構…），也不希望每多一種新的走訪用途，就得回頭在聚合物件本身開一個新方法`

**優點**：

- 呼叫端不用管底層資料結構長什麼樣，統一用同一種方式走訪（`foreach`、或語言提供的迭代語法）
- 走訪邏輯集中在迭代器自己身上，不會散落在每一個想走訪這個聚合物件的呼叫端，各自重寫一次遞迴或索引邏輯
- 同一個聚合物件可以同時存在多個獨立的走訪過程，彼此互不干擾，各自記著自己走到哪

**缺點**：

- 主流語言大多把這個 Pattern 內建進語言基礎設施（`foreach`／`for...of`／生成器），日常業務程式碼很少需要自己手刻一個，真正動手實作的情境比其他 Pattern 少見
- 走訪過程中如果底層集合的內容被修改，容易跟走訪狀態不同步，需要額外設計因應

---

明天，30 天的最後一篇，來做整個系列的總結，也會順便聊聊 Memento、Visitor、Interpreter 這三個 Pattern，為什麼這次沒有另外抽一天講

## 參考資料

[Refactoring Guru - Iterator](https://refactoring.guru/design-patterns/iterator)

[Microsoft Docs - IEnumerable\<T\> Interface](https://learn.microsoft.com/en-us/dotnet/api/system.collections.generic.ienumerable-1)

[C# Iterators](https://learn.microsoft.com/en-us/dotnet/csharp/iterators)
