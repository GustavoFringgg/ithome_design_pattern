# Day 8｜SOLID 的取捨與衝突

到這裡，SOLID 五個原則總算走完一輪了

這篇不談新原則，來看看這五個原則湊在一起會不會打架，還有 KISS、YAGNI、DRY 這三個更老牌的原則，怎麼制衡「用力過猛的 SOLID」

## 先說結論

SOLID 每一條單獨看都對，`OrderCalculator` 該不該只算錢、`BuyOneGetOneDiscount` 該不該乖乖實現 `IDiscount` 的承諾，這些道理都沒問題

但如果**每一條都做到極致**，甚至同時做到極致，很容易互相打架，最後長出一套「語法上完全遵守 SOLID，但沒有人看得懂、也沒有人改得動」的系統

這正是 AI 很容易犯的錯——AI 認得 SOLID 的名字、認得該長什麼樣子，卻不知道「什麼時候該停手」

## 老樣子，我們先認識一下今天的三個新面孔

**KISS(Keep It Simple, Stupid)**

源自美國海軍 1960 年代的工程設計原則，後來被軟體業借用，講的是：**能簡單解決的問題，就不要故意做複雜**

不必要的複雜度，本身就是 bug 溫床——多一層抽象，就多一個可能出錯、也多一個要維護的地方

**YAGNI(You Aren't Gonna Need It)**

出自 Extreme Programming(極限編程)，由 Ron Jeffries 提出，講的是：**不要為了「將來可能用得到」而現在就先做**

現在多包的那層彈性，十次有九次，等真的需求來的時候，會發現當初猜錯方向，那層彈性反而變成要先拆掉的包袱

**DRY(Don't Repeat Yourself)**

出自《The Pragmatic Programmer》，Andy Hunt 與 Dave Thomas 提出，原文是：

> Every piece of knowledge must have a single, unambiguous, authoritative representation within a system.

常常被簡化成「不要寫重複的程式碼」，但這裡容易誤解——DRY 講的是**同一份「知識」**不該有兩份，不是**長得像的程式碼**不該有兩份

兩段程式碼現在剛好都是 `price * 0.9m`，不代表它們是同一份知識，硬把它們合併成同一個共用方法，明天兩邊的規則各自演化，反而會綁死彼此

好，帶著這三個新面孔，我們回到柴咖啡

## 阿柴的新習慣

SOLID 學完之後，阿柴變得很有紀律，每次寫 class 前都會先問自己一輪：「這樣有沒有符合 SRP？有沒有符合 OCP？依賴的是抽象還是具體？」

小黑提了一個很小的需求：「早上七點到九點，點美式送一片餅乾，我們叫它『晨喚組合』」

阿柴打開 `CheckoutService`，想著「這是促銷，該比照 `IDiscount` 的做法，用擴充取代修改」，於是動手：

```csharp
public interface IPromotionFactory
{
    IDiscount Create(PromotionConfig config);
}

public class DiscountFactoryProvider
{
    private readonly Dictionary<string, IPromotionFactory> _factories;

    public DiscountFactoryProvider(Dictionary<string, IPromotionFactory> factories)
        => _factories = factories;

    public IPromotionFactory Resolve(string type) => _factories[type];
}

public interface IPromotionConfigRepository
{
    PromotionConfig GetConfig(string promotionCode);
}

public class MorningComboDiscountFactory : IPromotionFactory
{
    private readonly IPromotionConfigRepository _configRepository;

    public MorningComboDiscountFactory(IPromotionConfigRepository configRepository)
        => _configRepository = configRepository;

    public IDiscount Create(PromotionConfig config) => new MorningComboDiscount(config);
}

public class MorningComboDiscount : IDiscount
{
    private readonly PromotionConfig _config;
    public MorningComboDiscount(PromotionConfig config) => _config = config;

    public decimal Apply(Order order, decimal currentTotal)
    {
        // 判斷是不是早上七點到九點、有沒有點美式……略
        return currentTotal;
    }
}
```

`IPromotionFactory`、`DiscountFactoryProvider`、`IPromotionConfigRepository`——為了「送一片餅乾」這個需求，多出了三個新介面、三個新 class

寫完自己也覺得怪怪的，但轉念一想：「這樣以後促銷規則要動態設定、要換資料來源，都不用改程式碼了吧」，就這麼交出去了

## Code Review 那天

資深同事看完這次的 PR，只問了一句：「送一片餅乾，為什麼要改八個檔案？」

阿柴一時語塞：「因為……要符合 OCP，未來新增促銷不用改舊程式碼，也要符合 DIP，依賴抽象不依賴具體」

同事：「那你有幾種『促銷設定的來源』？現在有除了寫死在程式碼以外的第二種嗎？」

阿柴：「……沒有，目前只有這一種」

同事：「那 `IPromotionConfigRepository` 現在只有一個實作，也沒人會拿假的版本替換它去測試，包這層抽象是在保護什麼？」

阿柴突然說不出話來，回頭看了看小黑最早的需求：「早上送一片餅乾」，跟眼前這八個檔案，中間好像隔了太多層

## 那問題出在哪呢

單獨看每一個決定，都能找到一條 SOLID 撐腰：

- 包 `IPromotionFactory`，是為了 OCP——以後新增促銷方式，不用改舊程式碼
- 包 `IPromotionConfigRepository`，是為了 DIP——依賴抽象，不依賴具體的設定來源
- 拆出 `DiscountFactoryProvider` 專門負責「找出對應的 factory」，是為了 SRP——每個 class 只做一件事

問題不是「這些原則錯了」，是阿柴把每一條原則都當成「越多越好」在套，卻沒有回頭問一句：

**這一層抽象，現在真的有第二種情況需要它嗎？**

`IPromotionConfigRepository` 只有一種實作，未來會不會真的換資料來源，是阿柴自己猜的——這正是 YAGNI 要擋下的狀況：為了一個還沒發生、也不確定會不會發生的「未來」，現在就先付出複雜度的代價

而且，光是要看懂「送一片餅乾」這條規則實際跑在哪，得先追過 `DiscountFactoryProvider` 找 factory、再讓 factory 讀 `IPromotionConfigRepository`、才真正走到 `MorningComboDiscount.Apply()`——中間繞的每一圈，都是 KISS 在提醒的：**能一步到位的事，不要故意繞路**

## 那我們該安全處理呢

先問三個問題，把不必要的層拆掉：

1. `IPromotionConfigRepository` 現在只有一種實作，也沒有測試需要替換它——不符合「未來可能被替換、或需要在測試被隔離」的標準，拿掉
2. `IPromotionFactory` / `DiscountFactoryProvider` 本質上只是在做 `new MorningComboDiscount()`，多包一層 factory 沒有解決任何「不知道該 new 誰」的問題，拿掉
3. 剩下真正該留的，只有 `IDiscount` 這個已經在 Day5、Day6 驗證過、確實會有多種實作(打折、買一送一、點數折抵……)的抽象

改完之後，回到最單純的樣子：

```csharp
public class MorningComboDiscount : IDiscount
{
    public decimal Apply(Order order, decimal currentTotal)
    {
        var isMorningWindow = DateTime.Now.Hour is >= 7 and < 9;
        var hasAmericano = order.Items.Any(i => i.DrinkType == DrinkType.Americano);

        if (isMorningWindow && hasAmericano)
        {
            Console.WriteLine("送一片餅乾");
        }

        return currentTotal; // 這個促銷不影響金額，只影響要不要送餅乾
    }
}
```

新增一個促銷，改動的檔案數量，從八個變回一個，`CheckoutService` 完全沒被動到——`IDiscount` 這個抽象本身沒有錯，錯的是阿柴在它旁邊，多蓋了三層根本沒人住的房間

## 用 KISS / YAGNI 當照妖鏡

下次 AI 生出一段程式碼，可以反問自己：

- **這個 interface 現在有幾種實作？** 只有一種，而且想不到第二種會是誰、也沒人會拿假的版本測試它，那大概率是 YAGNI 要擋的訊號
- **這個小需求，改動的檔案數量，跟需求本身的大小成比例嗎？** 送一片餅乾改八個檔案，比例明顯不對
- **要看懂這段邏輯實際做了什麼，需要追過幾層才看得到？** 追超過兩三層還沒看到真正在做事的程式碼，通常代表抽象包過頭了

## SOLID 與 KISS / YAGNI / DRY 的邊界在哪

那是不是乾脆別用 SOLID，全部寫成一個 class 到底，才是最 KISS 的做法？

NO NO NO，Day3 的 `OrderService` 已經示範過另一個極端——什麼都塞在一起，改一個地方炸另一個地方，那是技術債，不是簡單

**SOLID 負責回答「該不該拆」，KISS / YAGNI / DRY 負責回答「現在該拆到哪一層就好」**

判斷標準常被稱為 **Rule of Three**：第一次先照需求老實寫，第二次類似的情況出現時可以先觀察，等真的**第三次**看到同樣的變化模式，再抽出抽象——比起「先猜未來、先包好抽象」，等真正的重複出現兩三次再動手，抽出來的抽象通常準得多

`IDiscount` 之所以站得住腳，是因為它撐過了三種真實出現的促銷(打折、買一送一、點數折抵)；`IPromotionConfigRepository` 撐不住，是因為它只是阿柴自己猜的「未來可能會有」

## 容易跟哪個概念搞混

- **過度工程 vs 技術債**：這是天平的兩端，Day3 的 `OrderService` 是技術債(該拆沒拆)，這篇的 `IPromotionFactory` 是過度工程(不該拆卻拆了)，SOLID 沒抓好分寸，兩邊都有可能發生
- **DRY vs SRP**：兩者有時會互相拉扯。為了不重複(DRY)，把兩段長得像、但其實屬於不同職責的邏輯合併成一個共用方法，反而會違反 SRP——之後這兩個地方的規則各自演化，改一邊會不小心動到另一邊，這時候「留兩份像的程式碼」通常比「硬凑成一份」更安全

## 違反時，系統會出現什麼症狀

- 一個很小的需求，得改動遠超過需求本身大小的檔案數量
- 出現只有一個實作的 interface，沒人會替換它，也沒有測試在用假的版本頂替
- 要追三、四層才找得到「真正在做事」的那行程式碼
- Code Review 常聽到「這也太多層了吧」「這個需求有必要包這麼多嗎」

## AI 產出程式碼時，這幾個原則能拿來檢查什麼

下次 AI 生出一段程式碼，可以先問自己：「這個抽象，是為了現在真的存在的需求，還是為了我自己猜的、還沒發生的未來？」

如果答案是後者，那大概率就是 SOLID 用力過猛、被 YAGNI 擋下的訊號

**SOLID 該保護的是「未來變動時，改起來局部、安全」，不是「現在就把所有想像得到的彈性都先做出來」**

---

到這裡，SOLID 五原則加上 KISS、YAGNI、DRY 這套判斷工具，總算集滿了

下一篇開始，阿柴咖啡要正式進料開張，我們也要進入創建型 Pattern 的第一站：**Factory Method**——來看看阿柴怎麼生出拿鐵、美式、摩卡

## 參考資料

[wiki - KISS](https://en.wikipedia.org/wiki/KISS_principle)

[wiki - YAGNI](https://en.wikipedia.org/wiki/You_aren%27t_gonna_need_it)

[wiki - DRY](https://en.wikipedia.org/wiki/Don%27t_repeat_yourself)

[Martin Fowler - Yagni](https://martinfowler.com/bliki/Yagni.html)

[Blog](https://blog.cleancoder.com/)
