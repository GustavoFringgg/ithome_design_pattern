# Day 8｜SOLID 的取捨與衝突

今天阿柴咖啡店休，不繼續故事，單純聊邏輯

SOLID 五個原則講完了，這篇想回頭談一件事：**這五條原則放在一起，會不會打架？**

## 每一條單獨看都對，湊在一起呢

DIP 說「依賴抽象，不依賴具體」，OCP 說「用擴充取代修改」，SRP 說「一個 class 只做一件事」——這幾條原則，各自拿出來看都站得住腳，Day3 到 Day7 也一路示範過

但如果**每一條都做到極致，還同時做**，會發生什麼事？我們用 Day7 那組 `Switch` / `Bulb` / `ISwitchable` 來試試看

假設現在多一個很小的需求：**開關被切換的時候，順便印一行紀錄**

最直接的寫法：

```csharp
public class Switch
{
    private readonly ISwitchable _device;

    public Switch(ISwitchable device) => _device = device;

    public void Toggle(bool on)
    {
        if (on) _device.TurnOn();
        else _device.TurnOff();

        Console.WriteLine($"{DateTime.Now}: 開關被切換為 {(on ? "開" : "關")}");
    }
}
```

三行搞定。但如果照 SOLID 每一條都套到底，會長成這樣：

```csharp
// SRP：把「格式化文字」這件事,獨立成自己的職責,不跟寫入邏輯混在一起
public interface ILogEntryFormatter
{
    string Format(bool on, DateTime time);
}

public class DefaultLogEntryFormatter : ILogEntryFormatter
{
    public string Format(bool on, DateTime time) => $"{time}: 開關被切換為 {(on ? "開" : "關")}";
}

// OCP + DIP：以後想把紀錄寫進檔案、傳到遠端,只要新增一個實作,不用改 ToggleLogger
public interface ILogWriter
{
    void Write(string entry);
}

public class ConsoleLogWriter : ILogWriter
{
    public void Write(string entry) => Console.WriteLine(entry);
}

// DIP：讓 Switch 不需要認得「怎麼記錄」的具體做法,只認得這份抽象
public interface IToggleLogger
{
    void LogToggle(bool on);
}

// SRP：專門負責「格式化 + 寫入」怎麼串起來這件事,跟 Switch 本身的開關邏輯分開
public class ToggleLogger : IToggleLogger
{
    private readonly ILogEntryFormatter _formatter;
    private readonly ILogWriter _writer;

    // DIP：建構式吃的是 ILogEntryFormatter / ILogWriter 這兩個抽象,不是具體類別
    public ToggleLogger(ILogEntryFormatter formatter, ILogWriter writer)
    {
        _formatter = formatter;
        _writer = writer;
    }

    public void LogToggle(bool on) => _writer.Write(_formatter.Format(on, DateTime.Now));
}

public class Switch
{
    private readonly ISwitchable _device;
    private readonly IToggleLogger _logger; // DIP：依賴 IToggleLogger 這個抽象,不是具體的 ToggleLogger

    public Switch(ISwitchable device, IToggleLogger logger)
    {
        _device = device;
        _logger = logger;
    }

    public void Toggle(bool on)
    {
        if (on) _device.TurnOn();
        else _device.TurnOff();

        _logger.LogToggle(on);
    }
}
```

一行 `Console.WriteLine`，變成三個新介面、三個新 class

## 每一層都能找到一條原則撐腰

這才是麻煩的地方——沒有一層是亂包的：

- `ILogEntryFormatter` 分出來，是 SRP：「格式化文字」跟「寫到哪裡去」是兩件事
- `ILogWriter` 抽成介面，是 DIP：以後想把紀錄寫進檔案、傳到遠端，不用改 `ToggleLogger`
- `IToggleLogger` 抽成介面，也是 DIP：`Switch` 不該認得具體的 logger 實作
- 每一層都不用改舊程式碼就能擴充，也符合 OCP

單獨審查每一個決定，都能講出一套道理。問題是**沒有人問一句：現在，`ILogEntryFormatter` 真的有第二種格式嗎？`ILogWriter` 真的有第二種寫入方式嗎？**

答案都是沒有。這正是 YAGNI(You Aren't Gonna Need It) 想擋下的狀況——為了一個還沒發生、也不確定會不會發生的「未來」，現在就先付出三層抽象的代價

而且，光是想知道「開關被切換時，到底印出什麼字」，得先追過 `ToggleLogger` 找到 `_formatter`，再追到 `DefaultLogEntryFormatter.Format()`，才看得到那行字串組出來的樣子——這正是 KISS(Keep It Simple, Stupid) 在提醒的：**能一步到位看到答案的事，不要故意讓人繞三層**

## KISS、YAGNI、DRY 是什麼

三個都比 SOLID 資歷更老，也更直觀：

**KISS(Keep It Simple, Stupid)**

源自美國海軍 1960 年代的工程設計原則，後來被軟體業借用：**能簡單解決的問題，就不要故意做複雜**。不必要的複雜度本身就是 bug 溫床——多一層抽象，就多一個可能出錯、也多一個要維護的地方

**YAGNI(You Aren't Gonna Need It)**

出自 Extreme Programming(極限編程)，由 Ron Jeffries 提出：**不要為了「將來可能用得到」而現在就先做**。現在多包的那層彈性，十次有九次，等真的需求來的時候，會發現當初猜錯方向，那層彈性反而變成要先拆掉的包袱

**DRY(Don't Repeat Yourself)**

出自《The Pragmatic Programmer》，Andy Hunt 與 Dave Thomas 提出，原文是：

> Every piece of knowledge must have a single, unambiguous, authoritative representation within a system.

常被簡化成「不要寫重複的程式碼」，但這裡容易誤解——DRY 講的是**同一份「知識」**不該有兩份，不是**長得像的程式碼**不該有兩份。兩段程式碼現在剛好都是 `level * 10`，不代表它們是同一份知識，硬把它們合併成同一個共用方法，明天兩邊的規則各自演化，反而會綁死彼此(下面馬上示範)

## 拿掉不必要的層

回到 `ToggleLogger` 那三層，用 YAGNI 檢查一輪：

1. `ILogEntryFormatter` 只有一種格式，沒人會換掉它，也沒有測試需要替身——拿掉，格式化邏輯直接寫在需要的地方就好
2. `ILogWriter` 只有一種寫法(印到 console)，沒有第二種實作在等——拿掉
3. `IToggleLogger` 本質上只是包了一層轉呼叫，拿掉之後 `Switch` 直接做這件事就好

改完，回到最開始那三行：

```csharp
public class Switch
{
    private readonly ISwitchable _device;

    public Switch(ISwitchable device) => _device = device;

    public void Toggle(bool on)
    {
        if (on) _device.TurnOn();
        else _device.TurnOff();

        Console.WriteLine($"{DateTime.Now}: 開關被切換為 {(on ? "開" : "關")}");
    }
}
```

`ISwitchable` 沒有被拿掉，因為它撐得住——`Bulb`、`Fan` 是兩個真實存在、行為也真的不一樣的實作，`Switch` 依賴這個抽象，是為了「現在」的需求，不是為了猜的「未來」

## SOLID 原則彼此，也會自己打架

不是只有「SOLID vs YAGNI」這種外部制衡，SOLID 內部五條原則之間，有時候也會互相拉扯，DRY 跟 SRP 是最常見的一組

假設 `Bulb` 的亮度、`Fan` 的轉速，現在剛好都是同一個公式：

```csharp
public class Bulb : ISwitchable
{
    public int Brightness(int level) => level * 10; // 每階 10%
}

public class Fan : ISwitchable
{
    public int Speed(int level) => level * 10; // 每階 10%
}
```

長得一模一樣，套 DRY 的直覺，會想把它們合併成一個共用方法：

```csharp
public static class PercentageCalculator
{
    public static int Calculate(int level) => level * 10;
}
```

`Bulb.Brightness()` 跟 `Fan.Speed()` 都改成呼叫 `PercentageCalculator.Calculate()`，程式碼確實變少了

但過一陣子，`Bulb` 換成 LED 燈條，需要加一段非線性的亮度曲線(人眼對暗部比較敏感，低階位要調得更平滑)，`Fan` 的轉速邏輯完全不受影響，還是線性

這時候 `PercentageCalculator` 要嘛被迫加一個 `isBulb` 這種參數去分岔邏輯，要嘛乾脆被拆回兩份——繞了一圈，才發現「長得一樣」的兩段程式碼，其實從來不是「同一份知識」，只是剛好現在算出來的數字一樣。硬合併成一份，是把 SRP 犧牲給了 DRY：`PercentageCalculator` 本來該只有一個改變的理由，現在變成「燈泡的曲線變了」跟「風扇的轉速規則變了」都會讓它跟著改

這種情況下，**留兩份看起來重複的程式碼，通常比硬湊成一份更安全**，這也是為什麼 DRY 的原文特別強調「knowledge」而不是「code」

## 那到底該拆到哪一層

判斷標準常被稱為 **Rule of Three**：第一次先照需求老實寫，第二次類似的情況出現時可以先觀察，等真的**第三次**看到同樣的變化模式，再抽出抽象

比起「先猜未來、先包好抽象」，等重複真正出現兩三次再動手，抽出來的抽象通常準得多——`ISwitchable` 之所以站得住腳，是因為 `Bulb` 跟 `Fan` 是兩個已經真實存在、行為也真的不同的裝置，不是阿柴自己猜的「以後可能會有別的裝置」

**SOLID 負責回答「該不該拆」，KISS / YAGNI / DRY 負責回答「現在該拆到哪一層就好」**——兩邊搭配著用，才不會走向兩個極端：要嘛什麼都塞在一個 class 裡(技術債)，要嘛什麼都拆成三層抽象(過度工程)

## 對 AI 產出的程式碼，這件事特別重要

AI 很擅長把 SOLID 的名字、該長什麼樣子背得滾瓜爛熟——給它一個「印一行 log」的需求，它很可能直接生出前面那個三層抽象的版本，因為每一層都能講出對應的原則，語法也完全正確

但它不會主動問：「這個 `ILogWriter`，現在真的有第二種寫法在排隊嗎？」「這個 `IPromotionConfigRepository`，未來真的會換資料來源，還是我自己在腦補？」

這正是整個系列想強調的判斷力——**AI 給得出語法，給不出取捨**。SOLID 教我們「怎麼拆」，KISS / YAGNI / DRY 教我們「現在該不該拆」，兩邊都懂，才算真的懂 SOLID，而不是背下五個關鍵字去到處套用

---

明天阿柴咖啡店重新開張，我們也要正式進入創建型 Pattern 的第一站：**Factory Method**——來看看阿柴怎麼生出拿鐵、美式、摩卡

## 參考資料

[wiki - KISS](https://en.wikipedia.org/wiki/KISS_principle)

[wiki - YAGNI](https://en.wikipedia.org/wiki/You_aren%27t_gonna_need_it)

[wiki - DRY](https://en.wikipedia.org/wiki/Don%27t_repeat_yourself)

[Martin Fowler - Yagni](https://martinfowler.com/bliki/Yagni.html)

[Blog](https://blog.cleancoder.com/)
