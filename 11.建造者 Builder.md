# Day 11｜Builder——客製化飲料(少冰、半糖、加燕麥奶)

今天說到 Builder 建造者模式

我們來看看原文的定義，來自 Design Patterns: Elements of Reusable Object-Oriented Software

> Builder is a creational design pattern that lets you construct complex objects step by step. The pattern allows you to produce different types and representations of an object using the same construction code.

可以解釋成

1. 複雜物件不要一次到位塞給建構子，改成一步一步組
2. 同一套組裝步驟，可以組出長得不一樣的最終結果

抑或是

> 當一個物件太複雜、參數太多時，傳統建構方式會讓程式碼變得極難維護與容易出錯

所以主要也是要解決

1. 建構複雜度
2. 物件狀態完整性

你是不是在想每個字我都知道，怎麼湊在一起我就不知道了...沒關係朋友我也是...

用簡單個例子來說明，我很喜歡吃漢堡，那就拿漢堡來舉例

```csharp
public class Burger
{
    public string Bread { get; }
    public string Patty { get; }
    public bool HasCheese { get; }
    public bool HasBacon { get; }
    public bool HasLettuce { get; }
    public bool HasTomato { get; }

    public Burger(string bread, string patty, bool hasCheese, bool hasBacon, bool hasLettuce, bool hasTomato)
    {
        Bread = bread;
        Patty = patty;
        HasCheese = hasCheese;
        HasBacon = hasBacon;
        HasLettuce = hasLettuce;
        HasTomato = hasTomato;
    }
}
```

好接下來我想要點一個漢堡，麻煩就來了

```csharp
// 我想點一個「只要加起司和生菜」的漢堡
var myBurger = new Burger("麵包", "牛肉", true, false, true, false);
```

這時候帶來三個痛點

1. 看不懂: true false true false 分別代表什麼意思?
2. 萬一不小心把生菜跟番茄的 true/false 順序對調，編譯器不會警告你，自顧自地幫你加了番茄
3. 一定要填寫不需要的選項： 我只是不要培根，卻必須強制傳入 false，who cares 培根

恨不恨? 討不討厭?

那接下來，我們使用 Builder 進行簡化

我們"額外"在做一個 BurgerBuilder，讓它專門負責「幫忙點餐」：

```csharp
public class BurgerBuilder
{
    // 必要項目
    public string Bread { get; } // 麵包
    public string Patty { get; } // 漢堡徘

    // 選填項目（預設都是 false）
    public bool HasCheese { get; private set; }
    public bool HasBacon { get; private set; }
    public bool HasLettuce { get; private set; }
    public bool HasTomato { get; private set; }

    // 1. 建構子只放「一定要有」的東西
    public BurgerBuilder(string bread, string patty)
    {
        Bread = bread;
        Patty = patty;
    }

    // 2. 選填項目：要加什麼就呼叫對應的方法，每次都 return this
    public BurgerBuilder AddCheese()
    {
        HasCheese = true;
        return this; // 這裡 this 代表整個 BurgerBuilder 實體
    }

    public BurgerBuilder AddBacon()
    {
        HasBacon = true;
        return this;
    }

    public BurgerBuilder AddTomato()
    {
        HasTomato = true;
        return this;
    }

    // 3. 最後送出訂單，產出做好的漢堡
    public Burger Build()
    {
        return new Burger(Bread, Patty, HasCheese, HasBacon, HasLettuce, HasTomato);
    }
}

```

透過 builder 之後，那我們該怎麼再點相同漢堡呢?(只要起司跟生菜)

```csharp
var myBurger = new BurgerBuilder("麵包", "牛肉")
    .AddCheese()
    .AddTomato()
    .Build();
```

回到我們最初的 Builder 定義

1. 複雜物件不要一次到位塞給建構子，改成一步一步組
2. 同一套組裝步驟，可以組出長得不一樣的最終結果

是不是慢慢理解，Builder 改變了什麼?

1. 不需要傳一堆 false： 沒點的東西就不用寫

2. 可讀性提高： 看這行程式碼，想必都能理解我點了「麵包、牛肉排、加起司、加番茄」

3. 順序隨便你點： 先寫 .AddTomato() 再寫 .AddCheese() 結果完全一樣，不再被參數順序綁死

這裡就會有 class Burger(配方) 跟 class BurgerBuilder(服務生)

透過跟服務生點餐，所以也可以客製化(點飲料最愛客製了)

> 如果團隊更嚴謹的話，會直接將 Builder 寫在 Burger 裡面，Burger 建構子用 private 去處理<br>
> 只能透過 public Burger Build() 去生成物件，外部沒辦法擅自生成 Burger<br>
> 改用 var myBurger = new Burger.Builder("芝麻麵包", "牛肉")....呼叫

好多了嗎朋友，那我們帶這著感覺回到柴咖啡

前兩天用 Factory Method 跟 Abstract Factory，解決的都是「該生出哪一種(或哪一組)產品」的問題

也陸續解決小黑的問題，但隨著來客數提高，相對客人的問題也會複雜化

「半糖就好」、「去冰喔」、「可以換燕麥奶嗎」、「去冰微微」...

同一杯拿鐵，客製化選項一多，麻煩的已經不是「要生哪一種飲料」，而是「這一杯飲料，內部細節到底該怎麼一步步組合出來」

## 先看客製化出了什麼問題

阿柴打開小黃留下的程式碼

```csharp
public class Latte
{
    public string Size { get; }
    public string IceLevel { get; }
    public string SugarLevel { get; }
    public string MilkType { get; }

    // 是不是跟漢堡一樣很熟悉
    public Latte(string size, string iceLevel, string sugarLevel, string milkType)
    {
        Size = size;
        IceLevel = iceLevel;
        SugarLevel = sugarLevel;
        MilkType = milkType;
    }
}
```

呼叫端要點一杯「大杯、少冰、半糖」，其他用預設就好，但建構子只有一種版本，五個參數一個都不能少

```csharp
var latte = new Latte("大杯", "少冰", "半糖", "鮮奶");
```

小黑後來反應：「很多客人根本不管奶的種類，幹嘛每次都要打鮮奶」

小黃於是又加了幾個建構子，讓沒特別交代的選項可以吃預設值

```csharp
public Latte(string size, string iceLevel, string sugarLevel)
    : this(size, iceLevel, sugarLevel, "鮮奶") { }

public Latte(string size, string iceLevel)
    : this(size, iceLevel, "全糖", "鮮奶") { }

public Latte(string size)
    : this(size, "正常冰", "全糖", "鮮奶") { }
```

這種「一個建構子疊一個建構子」的寫法，業界叫做**telescoping constructor(電話線建構子)**，是新手常見的入門寫法

阿柴光看就頭皮發麻，先不論之後選項只會越加越多，建構子的排列組合永遠補不完

更麻煩的是，`iceLevel`、`sugarLevel`、`milkType` 全部都是 `string`，型別上完全無法分辨誰是誰

```csharp
// 這行語法完全合法，C# 編譯器不會報一個字的錯
var latte = new Latte("大杯", "半糖", "少冰");
// 但實際上冰量跟糖量被寫反了，客人拿到「半糖冰、少冰糖」的謎片飲料
```

點餐的工讀生一忙、順序一背錯，錯的訂單就這樣被送進廚房，而且沒有任何機制會提醒他錯了

## Builder 建造者上場

Factory Method 跟 Abstract Factory 處理的是「該挑哪一個(或哪一組)具體類別」

阿柴現在遇到的問題不一樣：**同一種飲料，可選的客製化細節太多，用建構子一次塞完既難讀又容易兜錯**

這正是 Builder 要處理的核心問題

> 將一個複雜物件的建構過程，與它最終的表示方式分離，讓相同的建構過程可以組出不同的表示結果

一樣拆開來看

1. 「建構過程與表示分離」=> 「怎麼一步步組裝」交給專門的建造者，「組出來的成品」則是另一個乾淨的類別，兩件事分開
2. 「相同建構過程組出不同表示」=> 同一套「選尺寸、選冰糖、選奶」的流程，可以組出千百種不同客製化組合的拿鐵
3. 呼叫端不用背順序 => 只要呼叫「有語意的方法名稱」，不用管內部欄位怎麼存放、順序怎麼排

阿柴這次改用**流暢建造者(Fluent Builder)**的寫法，是業界最常見的變形版：每個設定方法都回傳自己(`this`)，方便串接呼叫

```csharp
// ==========================================
// 1. 產品層（Product）：組好後就不再變動，避免半成品狀態外流
// ==========================================
public class CustomLatte
{
    public string Size { get; }
    public string IceLevel { get; }
    public string SugarLevel { get; }
    public string MilkType { get; }

    // 只有 Builder 會呼叫這個建構子，客戶端不再直接 new
    public CustomLatte(string size, string iceLevel, string sugarLevel, string milkType)
    {
        Size = size;
        IceLevel = iceLevel;
        SugarLevel = sugarLevel;
        MilkType = milkType;
    }

    public override string ToString()
        => $"{Size}拿鐵，{IceLevel}、{SugarLevel}、{MilkType}";
}

// ==========================================
// 2. 建造者（Builder）：一步一步設定客製化選項
// ==========================================
public class LatteBuilder
{
    // 預設值先給好，客人沒特別交代就用預設，不用每次都全部填滿
    private string _size = "中杯";
    private string _iceLevel = "正常冰";
    private string _sugarLevel = "全糖";
    private string _milkType = "鮮奶";

    public LatteBuilder SetSize(string size) { _size = size; return this; }
    public LatteBuilder SetIce(string iceLevel) { _iceLevel = iceLevel; return this; }
    public LatteBuilder SetSugar(string sugarLevel) { _sugarLevel = sugarLevel; return this; }
    public LatteBuilder SetMilk(string milkType) { _milkType = milkType; return this; }

    // 統一在這裡組裝，也是集中放驗證邏輯的好地方
    public CustomLatte Build()
    {
        return new CustomLatte(_size, _iceLevel, _sugarLevel, _milkType);
    }
}
```

呼叫端長這樣

```csharp
// 客人只想要「大杯、少冰、半糖」，其他吃預設值，方法名稱就是說明，不用背順序
var latte = new LatteBuilder()
    .SetSize("大杯")
    .SetIce("少冰")
    .SetSugar("半糖")
    .Build();

Console.WriteLine(latte);
// 輸出：大杯拿鐵，少冰、半糖、鮮奶

// 另一位客人要燕麥拿鐵、去冰、無糖
var oatLatte = new LatteBuilder()
    .SetIce("去冰")
    .SetSugar("無糖")
    .SetMilk("燕麥奶")
    .Build();
```

現在再也沒有「冰量跟糖量寫反」的問題，因為每一個值是透過語意明確的方法設定進去，不是靠位置硬記

也不用為了「有的客人不填某些選項」，就一路疊出五、六個 telescoping 建構子

## 那 GoF 課本裡的 Director 呢

Builder 模式的原始定義其實還有一個角色叫 **Director(指揮者)**，負責固定一套建構步驟的順序，反覆套用在不同 Builder 上

但阿柴發現，柴咖啡目前只有一種 Builder，也沒有「同一套流程要套用在多種不同建造者上」的需求，硬套一個 Director 類別只是多一層轉發

如果小黑之後想推出「本日特調」，永遠都是固定配方(大杯、少冰、半糖、燕麥奶)，比較實際的做法是包一個小方法，而不是照課本開一個正式的 Director 類別

```csharp
public static class LatteRecipes
{
    // 把常出現的固定套路包起來，比起硬開一個 Director 類別，更貼近現在的規模
    public static CustomLatte SignatureOatLatte()
        => new LatteBuilder()
            .SetSize("大杯")
            .SetIce("少冰")
            .SetSugar("半糖")
            .SetMilk("燕麥奶")
            .Build();
}
```

## 跟 Factory Method / Abstract Factory 差在哪

Factory Method 解決的是「這個東西該生出哪一種具體類別」，重點在**種類的選擇**

Abstract Factory 解決的是「一整組相關產品，怎麼保證一起生對」，重點在**產品之間的成套關係**

Builder 解決的是「同一種產品，內部組成的細節或可選參數太多」，重點在**建構過程本身要不要拆成多個步驟**，跟挑哪個具體類別完全無關，畢竟從頭到尾都只有一種 `CustomLatte`

這三者甚至可以疊在一起用：比如工廠方法內部決定要生產哪一種基底飲料，而這個基底飲料的客製化細節，則交給 Builder 一步步組出來，端看複雜度到底卡在哪一層

## 這個模式的代價

Builder 也不是沒有成本。阿柴發現，如果柴咖啡的客製化選項本來就只有一兩個，例如只有「要不要去冰」這種單一開關，其實 C# 內建的具名引數(named arguments)跟預設參數就綽綽有餘，不用多開一個 `LatteBuilder` 類別

```csharp
// 只有一兩個可選項時，具名引數 + 預設值就夠用，不必上 Builder
public class SimpleLatte
{
    public SimpleLatte(string size = "中杯", string iceLevel = "正常冰")
    {
        Size = size;
        IceLevel = iceLevel;
    }

    public string Size { get; }
    public string IceLevel { get; }
}

var latte = new SimpleLatte(iceLevel: "少冰"); // 只改冰量，可讀性也不差
```

多開一個 Builder 類別，等於多一層物件、多一層維護成本，選項不夠多時，這一層反而是多繞的路

## 邊界在哪

1. **可選參數的數量與組合，是不是真的多到讓一般建構子難以維護？** 只有一兩個開關，用具名引數或預設參數就好，硬上 Builder 是繞遠路

2. **建構過程之間，有沒有需要保護的順序或相依關係？** 如果各個選項彼此完全獨立，設定順序怎麼調都不影響結果，那 Builder 的價值主要只剩下「可讀性」；但如果有些選項互相牽動(例如某些品項不支援燕麥奶)，`Build()` 就是集中驗證這些規則的好地方

3. **物件在組裝過程中，會不會被其他程式碼意外讀到「組到一半」的狀態？** 如果程式是多執行緒環境，或物件在賦值途中就可能被別處存取，用 Builder 一次性組出不可變物件，會比暴露一堆 `public` setter 讓外部一步步改安全許多

## AI 產出程式碼時，Builder 能拿來檢查什麼

如果阿柴把「幫我做一個可以客製化冰糖奶跟大小的拿鐵訂單」丟給 AI，一樣容易看到兩種結果

保守版本，AI 直接生出跟小黃當年一樣的大建構子，把所有可選欄位塞進同一個參數列表，或是乾脆開一堆 `public` setter 讓外部自己一個一個改，前者遇到選項增加就要一路疊建構子，後者則讓物件隨時可能停在「只改了一半」的中間狀態被其他程式碼讀走

過度發揮版本，AI 可能直接照搬教科書，一次生出 `IBuilder` 介面、抽象 Builder 基底類別、外加正式的 `Director` 類別，但柴咖啡目前只有一種飲料、一種建造流程，也沒有「同一套組裝步驟要套用在多種 Builder 上」的需求，這其實是在為一個還沒出現的第二、第三種 Builder，先把介面跟指揮者都準備好

AI 給得出 Builder 該長什麼樣子，甚至能一次生出完整的建造者鏈式呼叫，但答不出「柴咖啡這幾個客製化選項，彼此有沒有相依關係、值不值得為了某個固定組合抽出一份 Recipe」——這件事得靠人自己盯著點餐習慣、盯著選項之間的關係去判斷，講清楚了，AI 給的 Builder 才會剛好卡在柴咖啡現在的複雜度，不多也不少

---

明天要來聊聊柴咖啡的菜單設定跟庫存，這種「全店只能有一份、不能各自為政」的資料，該怎麼設計才不會走歪，也順便聊聊 AI 常見的 **Singleton** 誤用

## 參考資料

[Refactoring Guru - Builder](https://refactoring.guru/design-patterns/builder)
