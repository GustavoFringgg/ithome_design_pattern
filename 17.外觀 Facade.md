# Day 17｜Facade——結帳流程包成簡單介面

昨天用 Adapter 把外送平台的資料格式接進柴咖啡的系統，今天我們繼續留在結構型 Pattern

來聊聊 Facade 外觀模式

原文的定義，一樣來自 Design Patterns: Elements of Reusable Object-Oriented Software

> Provide a unified interface to a set of interfaces in a subsystem. Facade defines a higher-level interface that makes the subsystem easier to use.

用中文理解可以是

> 為子系統中的一組介面，提供一個統一的高層介面，讓這個子系統更容易被使用

核心結構與觀念

1. 子系統各自獨立：每個子系統該做的事、該怎麼做，完全不受影響，Facade 不會動它們的內部邏輯
2. 統一入口：Facade 提供一個簡化過的高層方法，內部負責呼叫哪些子系統、用什麼順序呼叫
3. 不鎖死存取：呼叫端大部分時候走 Facade 就好，但真的有需要，還是可以直接繞過 Facade，找底層子系統

先舉個生活化的例子，週末窩在客廳追劇

家裡設備一堆，想看場 Netflix，得自己拿好幾支遙控器一步步操作：走去牆邊把燈光調暗、拿電視遙控器開電視切到 HDMI 2、拿擴大機遙控器開音響音量調到 30、拿 Apple TV 遙控器喚醒主機打開 Netflix

看完要關掉，還得把這四個動作倒過來全部按一遍——漏關一個，擴大機可能就通宵開著耗電

```csharp
// 四個各自獨立的子系統，各自有各自的開關方法，互不相干
public class Light
{
    public void Dim() => Console.WriteLine("燈光調暗");
}

public class Tv
{
    public void On() => Console.WriteLine("電視開啟，切到 HDMI 2");
    public void Off() => Console.WriteLine("電視關閉");
}

public class SoundBar
{
    public void On() => Console.WriteLine("音響開啟，音量調到 30");
    public void Off() => Console.WriteLine("音響關閉");
}

public class AppleTv
{
    public void PlayNetflix() => Console.WriteLine("Apple TV 喚醒，打開 Netflix");
    public void Stop() => Console.WriteLine("Apple TV 待機");
}
```

沒有 Facade 之前，呼叫端要自己記得四支遙控器、記得開跟關的順序

```csharp
var light = new Light();
var tv = new Tv();
var soundBar = new SoundBar();
var appleTv = new AppleTv();

// 看電影前，四個動作一個都不能漏
light.Dim();
tv.On();
soundBar.On();
appleTv.PlayNetflix();

// 看完之後，又要倒過來按一輪
appleTv.Stop();
soundBar.Off();
tv.Off();
```

Facade 把這串協調邏輯收進來，呼叫端只剩兩個方法可以叫

```csharp
public class HomeTheaterFacade
{
    private readonly Light _light;
    private readonly Tv _tv;
    private readonly SoundBar _soundBar;
    private readonly AppleTv _appleTv;

    public HomeTheaterFacade(Light light, Tv tv, SoundBar soundBar, AppleTv appleTv)
    {
        _light = light;
        _tv = tv;
        _soundBar = soundBar;
        _appleTv = appleTv;
    }

    public void WatchMovie()
    {
        _light.Dim();
        _tv.On();
        _soundBar.On();
        _appleTv.PlayNetflix();
    }

    public void EndMovie()
    {
        _appleTv.Stop();
        _soundBar.Off();
        _tv.Off();
    }
}
```

```csharp
var homeTheater = new HomeTheaterFacade(new Light(), new Tv(), new SoundBar(), new AppleTv());

homeTheater.WatchMovie(); // 一句話，四個設備依序就緒
homeTheater.EndMovie();   // 一句話，收乾淨，不會漏關
```

`Light`、`Tv`、`SoundBar`、`AppleTv` 這四個子系統完全沒被改動，Facade 只是站在它們前面，替呼叫端記住「這個情境該按哪些鍵、按什麼順序」

### 回到柴咖啡

柴咖啡結帳的當下，其實要做好幾件事：算這筆訂單多少錢、扣款（順便開發票、同步報表）、通知老闆有新訂單進來

這幾件事系列前面都各自處理過了：

- `OrderCalculator`：算金額，Day 3 從 `OrderService` 拆出來的
- `IPaymentProcessor`：扣款，Day 15 用 Decorator 包好的鏈——`InvoiceDecorator`、`SalesReportDecorator` 已經包在裡面，呼叫 `Charge()` 的同時，發票跟報表就一起處理掉了
- `LineNotifier`：通知老闆，Day 3 拆出來的

## 先看看阿柴趕工出來的版本

門市結帳的 Controller，直接把這三個子系統一個個接起來

```csharp
public class CheckoutController
{
    private readonly OrderCalculator _calculator;
    private readonly IPaymentProcessor _paymentProcessor;
    private readonly LineNotifier _notifier;

    public CheckoutController(OrderCalculator calculator, IPaymentProcessor paymentProcessor, LineNotifier notifier)
    {
        _calculator = calculator;
        _paymentProcessor = paymentProcessor;
        _notifier = notifier;
    }

    public async Task Checkout(Order order)
    {
        var total = _calculator.Calculate(order);
        var result = _paymentProcessor.Charge(order.OrderId, (int)total);
        await _notifier.Notify(total);
    }
}
```

上線前勉強能動，但沒多久，小黑說 App 點餐上線了，App 那邊的結帳也要走一樣的流程——阿柴一看，`App` 的 `CheckoutController` 又得把「算金額 → 扣款 → 通知」這三行複製一份過去

過沒多久，內用平板點餐也上線了，第三份一樣的三行程式碼又出現了

某天小黑說：「扣款前想先檢查一下這筆訂單有沒有問題，加個防呆」，阿柴才發現，這個判斷要塞回這三行邏輯的最前面，得同時改門市、App、內用平板，三個地方一個都不能漏改

問題不是阿柴手腳不夠快，而是**「結帳要做哪些事、用什麼順序做」這件事，被複製貼上到每一個呼叫端，沒有一個共同的地方可以改**

## Facade 上場

阿柴把這三行協調邏輯，收進一個新的 `CheckoutFacade`

```csharp
public class CheckoutFacade
{
    private readonly OrderCalculator _calculator;
    private readonly IPaymentProcessor _paymentProcessor;
    private readonly LineNotifier _notifier;

    public CheckoutFacade(OrderCalculator calculator, IPaymentProcessor paymentProcessor, LineNotifier notifier)
    {
        _calculator = calculator;
        _paymentProcessor = paymentProcessor;
        _notifier = notifier;
    }

    public async Task<PaymentResult> Checkout(Order order)
    {
        var total = _calculator.Calculate(order);
        var result = _paymentProcessor.Charge(order.OrderId, (int)total);
        await _notifier.Notify(total);
        return result;
    }
}
```

門市、App、內用平板的 Controller，全部改成只認識這一個入口

```csharp
public class CheckoutController
{
    private readonly CheckoutFacade _checkoutFacade;

    public CheckoutController(CheckoutFacade checkoutFacade)
    {
        _checkoutFacade = checkoutFacade;
    }

    public Task<PaymentResult> Checkout(Order order) => _checkoutFacade.Checkout(order);
}
```

小黑要加的「扣款前先檢查訂單」防呆，阿柴只要改 `CheckoutFacade.Checkout()` 這一個地方，門市、App、內用平板三邊完全不用動

## Facade 的取捨

**不鎖死底層子系統**：`OrderCalculator`、`IPaymentProcessor`、`LineNotifier` 都還是 `public`，後台想單獨補算一筆金額、單獨重發一次通知，還是可以直接繞過 `CheckoutFacade` 找它們，Facade 只是多開一條比較好走的路，不是唯一的路

**跟 SRP 會不會互相矛盾？** 阿柴一開始也有點疑惑，Day 3 才學過 SRP 要把職責拆開，今天 Facade 卻好像又把東西「合」了回去，這兩個原則會不會打架

其實不會。`OrderCalculator`、`IPaymentProcessor`、`LineNotifier` 各自的職責完全沒變，一個只算錢、一個只管扣款流程、一個只發通知；`CheckoutFacade` 自己也只有一個職責——**協調這三者的呼叫順序**，它不會自己動手算金額、不會自己扣款，只是負責「按對順序把三個電話打出去」

換句話說，SRP 管的是「每一個子系統自己該不該做超過一件事」，Facade 管的是「呼叫端該不該知道有這麼多子系統要協調」，兩者拆分的對象不一樣，並不衝突

## 今天學到的事

**Facade 要解決的問題**：

- 一個操作背後需要協調好幾個獨立的子系統，而且子系統之間有特定的呼叫順序
- 這串協調邏輯如果讓每個呼叫端各自處理，會被複製貼上到到處都是，改一次要抓好幾個地方
- 子系統的內部細節、正確的呼叫順序，不該是每個呼叫端都要自己搞懂的事

**優點**：

- 呼叫端只面對一個簡化過的入口，不用認識底層一堆子系統
- 協調邏輯集中在一處，順序要調整，只改 Facade 這一個地方
- 子系統本身不會被鎖死，還是可以個別被單獨呼叫，Facade 只是多開一條路，不是唯一的路

**缺點**：

- Facade 容易被不斷塞新功能，最後自己也變成什麼都做的「上帝物件」，違反它原本該遵守的單一職責
- Facade 通常只覆蓋最常見的那條路徑，呼叫端如果需要子系統更細緻的客製化操作，還是得繞過 Facade 直接找底層，這時候 Facade 反而成了一層沒用的殼
- 多一層轉呼叫，就多一個要維護、要測試的物件

---

明天來聊柴咖啡的會員優惠查詢，每次查詢都要連資料庫算一次，量一大系統就卡，這種「控制存取、加一層快取或權限檢查」的情境，輪到 **Proxy** 代理模式上場

## 參考資料

[Refactoring Guru - Facade](https://refactoring.guru/design-patterns/facade)
