# Day 3｜S(單一職責原則) 一個 class，只有一個改變的理由

## 阿柴開工了

Day 2 說好了，這 30 天要陪阿柴一起把柴咖啡系統從零蓋起來。

隔天，阿柴打開電腦，信心滿滿地對著 Codex 說：

> 「幫我寫一個咖啡點餐系統，要能建立訂單、計算金額、印出收據，還要傳 Line 通知我老闆，然後存進資料庫」

十秒後，蹦出來一個 `OrderService`，一個 class 全部搞定，跑起來完全沒問題

阿柴：「好猛，收工！」

## 好景不常

兩週後，老闆小黑說：「收據格式可以換一下嗎？我想加上店名跟日期」

阿柴：「好，小改而已 (挑眉」

改完之後，Line 通知突然壞掉了

阿柴：「？？？」

這就是 **單一職責原則（Single Responsibility Principle，SRP）** 要解決的問題

---

## 看看那個 class 長什麼樣

```csharp
public class OrderService
{
    public void ProcessOrder(Order order)
    {
        // 計算金額
        decimal total = order.Items.Sum(i => i.Price * i.Quantity);

        // 印出收據
        Console.WriteLine($"=== 柴咖啡收據 ===");
        Console.WriteLine($"小計：{total}");

        // 傳 Line 通知老闆
        var client = new HttpClient();
        client.PostAsync("https://notify-api.line.me/...", new StringContent($"新訂單！金額：{total}"));

        // 存進資料庫
        var db = new AppDbContext();
        db.Orders.Add(order);
        db.SaveChanges();
    }
}
```

一個 method，四件事：**計算、印收據、通知、存資料庫**

看起來很方便，問題是——這四件事，各自都有各自的「改變理由」：

- 老闆要換收據格式 → 動這裡
- Line API 改版 → 動這裡
- 換資料庫 → 動這裡
- 促銷活動要改計算邏輯 → 也動這裡

**每一次改動，都在對其他功能埋地雷**

---

## SRP 說什麼

> 一個 class，應該只有一個改變的理由

換句話說，一個 class 只負責一件事，當需求變動時，只有跟它有關的那個理由會讓它需要改。

---

## 拆開來長這樣

```csharp
// 只負責計算
public class OrderCalculator
{
    public decimal Calculate(Order order)
        => order.Items.Sum(i => i.Price * i.Quantity);
}

// 只負責印收據
public class ReceiptPrinter
{
    public void Print(Order order, decimal total)
    {
        Console.WriteLine($"=== 柴咖啡收據 ===");
        Console.WriteLine($"小計：{total}");
    }
}

// 只負責通知
public class LineNotifier
{
    public async Task Notify(decimal total)
    {
        var client = new HttpClient();
        await client.PostAsync("https://notify-api.line.me/...", new StringContent($"新訂單！金額：{total}"));
    }
}

// 只負責存資料
public class OrderRepository
{
    public void Save(Order order)
    {
        var db = new AppDbContext();
        db.Orders.Add(order);
        db.SaveChanges();
    }
}
```

現在老闆要換收據格式，只動 `ReceiptPrinter`，Line 通知不會受影響，資料庫也不會。

---

## 用 SRP 當照妖鏡

AI 非常容易生出「god class」——把所有邏輯塞在一個地方，因為這樣最快能動。

下次 AI 幫你生程式碼，可以問自己：

- **這個 class 有幾個改變的理由？** 超過一個，就該拆
- **如果需求 A 改了，會不會意外影響功能 B？** 會的話，就是職責混在一起了
- **這個 class 的名字能說清楚它做什麼嗎？** 叫 `OrderService` 但做了五件事，名字就是個謊言

---

## SRP 的邊界在哪

SRP 不是說「一個 class 只能有一個 method」，那是矯枉過正。

判斷標準只有一個：**這個 class 裡的所有邏輯，是不是為了同一個目的而存在？**

`ReceiptPrinter` 裡面可以有多個 method——`PrintHeader()`、`PrintItems()`、`PrintTotal()`，它們都為了「印收據」這一件事服務，這樣是沒問題的。

怕的是一個 class 同時在做「印收據」和「通知老闆」，因為這兩件事根本不是同一個理由。

---

下一篇，我們繼續看 SOLID 的第二條：**O——開放封閉原則**

阿柴的柴咖啡要開始加新口味了，看看不遵守 OCP 的後果有多慘 👀
