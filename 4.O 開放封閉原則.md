# Day 4｜O(開放封閉原則)

上一篇結尾提到，阿柴的柴咖啡要開始加新口味了~~

阿柴用 SRP 把 `OrderService` 拆成 `OrderCalculator`、`ReceiptPrinter`、`LineNotifier`、`OrderRepository` 四個各司其職的 class，本來以為可以鬆一口氣

沒想到，麻煩換了個位置出現

## 老闆的新口味

老闆小黑興沖沖地跑來：「最近抹茶紅了，我們也上一款抹茶拿鐵吧，買二送一衝業績！」

阿柴：「沒問題，飲料種類加個 case 就好」

打開 `OrderCalculator`，阿柴很快加了一個新分支，上線

兩天後，小黑又來了：「美式最近賣不好，加個常態九折試試？」

阿柴：「好」，又加了一個分支

又過一週，小黑：「抹茶拿鐵的買二送一取消，改成加購燕麥奶折 5 元」

阿柴這次改分支的時候，`手滑動到旁邊美式那段的判斷式，改完自己測了一下抹茶拿鐵`，沒事，就上線了

隔天，客訴進來：「我點美式，怎麼被多收錢？」

阿柴打開 log 一看，美式的九折邏輯，被自己上禮拜改抹茶拿鐵的時候不小心動到了

![Dog](./pic/D4/dog4.png)

## 我們來看看那段程式碼長什麼樣

```csharp
public class OrderCalculator
{
    public decimal Calculate(Order order)
    {
        decimal total = 0;

        foreach (var item in order.Items)
        {
            switch (item.DrinkType)
            {
                case DrinkType.Latte:
                    total += item.Price * item.Quantity;
                    break;

                case DrinkType.Americano:
                    total += item.Price * item.Quantity * 1.9m; // 老闆要常態九折，阿柴打成 1.9
                    break;

                case DrinkType.Mocha:
                    total += item.Price * item.Quantity;
                    break;

                // 新增口味都要擠進這裡，
                case DrinkType.MatchaLatte:
                    total += item.Price * item.Quantity - (item.OatMilkAddOn ? 5 : 0);
                    break;
            }
        }

        return total;
    }
}
```

問題不是「加了一個 case」，是**每一次新需求，都要回到同一個 method 裡面動刀**

一個 method 裡塞了三種飲料、兩種促銷邏輯，改任何一段，都有可能不小心波及旁邊那段——就像這次的美式九折

好 我們回到 SOLID 的 O — Open-Closed Principle (開放封閉原則 簡稱OCP)

## OCP 是什麼

由 Bertrand Meyer 在 1988 年提出，後來 Uncle Bob 在《Clean Architecture》裡進一步闡述：

> Software entities (classes, modules, functions) should be open for extension, but closed for modification.<br>
> 「軟體實體（類別、模組、函式）應該對擴充開放，對修改封閉」

> By 深入淺出設計模式<br>
> 類別應該歡迎擴展，但拒絕修改 <br>

白話一點：**新需求進來，應該用「新增程式碼」去滿足它，而不是回頭「修改既有程式碼」**<br>
或者說：**大多情況下，一個設計良好的技統應該在不需要修改的前提之下就可以輕易被擴展**

## 拆開來可以長這樣

把「每一種飲料怎麼算錢」抽成一個介面，飲料自己實作自己的算法，`OrderCalculator` 只負責找到對的算法、呼叫它，不用知道細節

```csharp
public interface IDrinkPricing
{
    bool CanHandle(DrinkType type);
    decimal Calculate(OrderItem item);
}

public class LattePricing : IDrinkPricing
{
    public bool CanHandle(DrinkType type) => type == DrinkType.Latte;
    public decimal Calculate(OrderItem item) => item.Price * item.Quantity;
}

public class AmericanoPricing : IDrinkPricing
{
    public bool CanHandle(DrinkType type) => type == DrinkType.Americano;
    public decimal Calculate(OrderItem item) => item.Price * item.Quantity * 0.9m;
}

public class MochaPricing : IDrinkPricing
{
    public bool CanHandle(DrinkType type) => type == DrinkType.Mocha;
    public decimal Calculate(OrderItem item) => item.Price * item.Quantity;
}

// 新口味上架，只要新增一個 class，不用碰美式、拿鐵的 code
public class MatchaLattePricing : IDrinkPricing
{
    public bool CanHandle(DrinkType type) => type == DrinkType.MatchaLatte;

    public decimal Calculate(OrderItem item)
    {
        var total = item.Price * item.Quantity;
        return item.OatMilkAddOn ? total - 5 : total;
    }
}

public class OrderCalculator
{
    private readonly IEnumerable<IDrinkPricing> _pricingRules;

    public OrderCalculator(IEnumerable<IDrinkPricing> pricingRules)
    {
        _pricingRules = pricingRules;
    }

    public decimal Calculate(Order order)
    {
        decimal total = 0;

        foreach (var item in order.Items)
        {
            var rule = _pricingRules.First(r => r.CanHandle(item.DrinkType));
            total += rule.Calculate(item);
        }

        return total;
    }
}
```

下次美式想改促銷方式，只動 `AmericanoPricing`，抹茶拿鐵、拿鐵完全不會被牽連

`OrderCalculator` 從頭到尾沒被打開過——這就是「對擴充開放，對修改封閉」

## 用 OCP 當照妖鏡

下次 AI 幫你生程式碼，可以問自己：

- **這次的新需求，是「新增」還是「修改」？** 如果每次都要回去改同一個 method，那個 method 可能就是 OCP 的破口

- **if-else / switch 是不是一直在同一個地方長大？** 像前面的 `OrderCalculator`，每上一款新飲料就多一個 case，這是很典型的訊號

  改成介面 + 各自實作後，新增變成「加一個 class」，不用再擠進同一段判斷式裡

- **改 A 需求的時候，會不會不小心動到 B？** 前面美式被多收錢的 bug，就是因為兩段邏輯擠在同一個 method，改一個手滑到另一個

## OCP 的邊界在哪

OCP 不是叫你一開始就把每個東西都包成介面，預留一堆「以後可能會變」的擴充點，那是矯枉過正，也是 AI 很愛犯的錯——問都不問，先把 `IStrategy`、`IFactory` 全部生一輪再說

判斷標準只有一個：**這裡「已經」變過幾次，而不是「可能」會變**

如果一款飲料上架後半年都沒改過算價方式，先不用急著抽象化；但像本篇的促銷邏輯，短短兩週改了三次，這種「已經在變、還會一直變」的地方，才是 OCP 該出手的地方

## OCP 容易跟哪個原則/Pattern 搞混

- **跟 Strategy Pattern**：OCP 是「目標」——對修改封閉；Strategy 是「達成目標的手段之一」——用多型把演算法抽換掉。前面 `IDrinkPricing` 的寫法，其實就是 Strategy 的雛形，Day 22 會再展開講

- **跟 SRP**：SRP 管的是「一個 class 該有幾個改變的理由」；OCP 管的是「當那個改變理由發生時，該用新增還是修改去應對」。兩者常常一起出現：職責先分乾淨（SRP），才有辦法針對單一職責做擴充（OCP）

## 違反 OCP 時，系統會出現什麼症狀

- if-else / switch case 越養越長，改一行都要全部飲料重新測一次
- 新增功能常常「順手」改壞舊功能，就像這次美式被多收錢
- Code Review 時，一個小需求的 diff 卻牽連一大片不相干的程式碼

## AI 產出程式碼時，這個原則能拿來檢查什麼

下次新需求進來，可以先問 AI 一句：「如果之後要再加一種飲料/促銷，會改到哪裡？」

如果答案是「要改這個 method 裡面的邏輯」，那大概率就是違反 OCP 的訊號

但也別走另一個極端——如果 AI 一開口就把所有東西都包成介面、抽象類別，先把擴充點全部生好，那也是問題：**OCP 該保護的是「已經在變的地方」，不是「所有可能變的地方」**，後者只是換一種方式過度工程

---

阿柴體悟到，AI 可以很快幫你把 if-else 串起來，也可以很快幫你把介面拆出來——但「這裡到底該不該現在就抽象化」，AI 不會幫你決定，那是阿柴自己要練的取捨

---

下一篇，我們繼續看 SOLID 的第三條：**L——里氏替換原則**

## 參考資料

[wiki](https://en.wikipedia.org/wiki/Open%E2%80%93closed_principle)
[好文](https://www.chouxiangpai.com/archives/2059/)
