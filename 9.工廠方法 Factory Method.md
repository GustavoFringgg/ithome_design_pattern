# Day 9｜Factory Method——生成不同飲料

新的一周，柴咖啡店開店了，菜單掛回去，拿鐵、美式、摩卡，老三樣

阿柴翻開小黃留下的舊程式碼,想說順手看一下點餐邏輯長怎樣,結果看到這個

```csharp
public class Drink
{
    public string Name { get; set; }
    public int Price { get; set; }
}

public class OrderService
{
    public Drink CreateDrink(string type)
    {
        if (type == "latte")
        {
            return new Drink { Name = "拿鐵", Price = 90 };
        }
        else if (type == "americano")
        {
            return new Drink { Name = "美式", Price = 70 };
        }
        else if (type == "mocha")
        {
            return new Drink { Name = "摩卡", Price = 100 };
        }
        else
        {
            throw new Exception("沒有這個飲料");
        }
    }
}
```

三種飲料,一個 if-else,能動,而且小黃當初也就只求能動

## 問題在哪

阿柴這幾天複習完 SOLID,職業病犯了,盯著這段程式碼看,腦中浮現 Day 4 開放封閉原則的那句話:

> 「對擴充開放,對修改封閉」

現在只有三種飲料,`CreateDrink` 這個方法還算好維護。但阿柴知道,小黑遲早會想加新飲料——例如上季討論過的「燕麥拿鐵」「冰滴咖啡」。每加一種,就要回來改這個 `if-else`,再往下加一段

短期內看起來沒差,但這個方法會一直長:三種變五種,五種變十種,每次改動都要在同一個方法裡動刀,改壞既有邏輯的機率也跟著往上疊

而且更麻煩的是,這個 `CreateDrink` 不會只被一個地方呼叫。等外送平台串進來(Day 15 會提到),Uber Eats 那邊送來的品項字串搞不好是 `"latte"`,foodpanda 又搞不好是 `"Latte"`——這種字串比對式的判斷,散在程式碼各處,改一次要抓好幾個地方,漏改一個都不會報錯,只會在某個客人點摩卡的時候,默默地給錯飲料

## Factory Method 上場

Factory Method 要處理的問題很單純:**把「決定要生成哪個物件」這件事,從呼叫端抽出來,交給專門的工廠方法負責**

核心概念是:先定義一個共同的產品介面(或抽象類別),每一種具體飲料各自實作;然後用一個工廠方法,依照條件回傳對應的實例,而不是把所有分支邏輯攤開寫在同一個方法裡

```csharp
public abstract class Drink
{
    public abstract string Name { get; }
    public abstract int Price { get; }
}

public class Latte : Drink
{
    public override string Name => "拿鐵";
    public override int Price => 90;
}

public class Americano : Drink
{
    public override string Name => "美式";
    public override int Price => 70;
}

public class Mocha : Drink
{
    public override string Name => "摩卡";
    public override int Price => 100;
}
```

工廠方法負責「生產」:

```csharp
public abstract class DrinkFactory
{
    public abstract Drink CreateDrink();
}

public class LatteFactory : DrinkFactory
{
    public override Drink CreateDrink() => new Latte();
}

public class AmericanoFactory : DrinkFactory
{
    public override Drink CreateDrink() => new Americano();
}

public class MochaFactory : DrinkFactory
{
    public override Drink CreateDrink() => new Mocha();
}
```

要加燕麥拿鐵的時候,阿柴只要新增一個 `Drink` 子類跟對應的 `DrinkFactory` 子類,原本三種飲料的程式碼一行都不用動——這就是「對擴充開放,對修改封閉」的具體長相,不是背名詞,是真的少改一個地方

還沒解決的問題是:「依照字串條件選出對應工廠」這件事,還是得有個地方做判斷——這通常會搭配一個小小的對照表或註冊機制,但那已經是實作細節,不是 Factory Method 本身要解的問題,這裡先不展開

## 這個原則的邊界在哪

Factory Method 不是「只要有 `new`,就該包一層工廠」

阿柴店裡如果飲料清單就是拿鐵、美式、摩卡,而且小黑明講「未來半年不會加新飲料,先求穩定」,那這三個 `new Drink(...)` 直接寫在該寫的地方就好,包一層工廠只是多一層要維護的抽象,沒有對應到任何實際的擴充需求

回到 Day 8 講過的 Rule of Three:阿柴不是憑感覺覺得「以後可能會加飲料」就動手包工廠,而是小黑已經明確提過燕麥拿鐵、冰滴咖啡兩個候補——變化的方向已經看得到了,這時候引入 Factory Method 才站得住腳,不然就是 YAGNI 講的、自己腦補未來需求

## 容易跟哪個搞混

Factory Method 最容易跟明天要講的 **Abstract Factory** 搞混,兩者的差別是規模:

- **Factory Method**:一個方法,決定「生產哪一個」單一產品(這杯是拿鐵還是美式)
- **Abstract Factory**:一個介面,一次生產「一整組相關」的產品家族(例如熱飲系列要配紙杯跟杯蓋,冷飲系列要配塑膠杯跟吸管,一次出貨一整組配件)

阿柴目前的問題只是「選一種飲料」,還沒有「一整組配件要一起換」的需求,所以先用 Factory Method 就夠,不需要直接跳去 Abstract Factory——這也是為什麼創建型 Pattern 要拆成兩天分開講,規模不一樣,解的問題也不一樣

## 違反時,系統會長什麼樣子

如果阿柴沒有做這次重構,繼續放著小黃原本的 `if-else`,症狀通常會是:

- 新增一種飲料,要跑去改好幾個地方的判斷式,改一次漏一次
- 同樣的「字串比對飲料種類」邏輯,在點餐、結帳、庫存扣款各自複製一份,各自演化出些微不同的寫法(這也是 Day 8 提過的 DRY 陷阱的前身)
- 想幫某一種飲料加特殊邏輯(例如摩卡要另外扣巧克力庫存),得先在一大串 `if-else` 裡找到正確的分支,改錯分支的風險一路都在

## AI 產出程式碼時,這個原則能拿來檢查什麼

如果阿柴把「幫我寫一個依照飲料種類建立訂單的功能」丟給 AI,常見會看到兩種極端:

第一種,AI 直接生出小黃當年那種一大包 `if-else`——能動,語法正確,但完全沒考慮到「這串邏輯以後還會不會變」

第二種,AI 反而過度熱心,一開口就是 `IDrinkFactory`、`AbstractDrinkFactory`、外加一個 `DrinkFactoryRegistry`,三種飲料硬是包出五六個類別——這是典型的**設計過頭**,因為它不知道阿柴店裡到底會不會真的擴充,它只知道「工廠模式看起來比較專業」

這裡能拿來檢查 AI 產出程式碼的問題只有一個:**現在真的有第二種、第三種產品在排隊,還是只是先包好以防萬一?** 如果連小黑都還沒說要加新飲料,AI 卻已經先幫你切好五層抽象,那多半是它在幫你腦補一個還沒發生的需求

AI 給得出「Factory Method 該長什麼樣子」的語法,但答不出「阿柴店裡現在到底要不要用」——這個判斷,還是得靠人自己盯著需求看

---

明天繼續留在小攤子階段,飲料種類要再往外擴一圈:熱飲、冷飲各自要配一整組不同的配件,一次生成,這時候就輪到 **Abstract Factory** 上場了

## 參考資料

[Refactoring Guru - Factory Method](https://refactoring.guru/design-patterns/factory-method)
