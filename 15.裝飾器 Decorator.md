# Day 15｜Decorator——扣款成功後的收尾動作(開立發票、列印明細單)

創建型五天王告一段落，今天我們正式進入結構型 Pattern（Structural）

第一個要來聊的是 Decorator 裝飾器模式

原文的定義，一樣來自 Design Patterns: Elements of Reusable Object-Oriented Software

> Attach additional responsibilities to an object dynamically. Decorators provide a flexible alternative to subclassing for extending functionality.

用中文理解可以是

> 動態地為一個物件附加額外的職責，裝飾器提供了比繼承更有彈性的方式，來擴充物件的功能

核心結構與觀念

1. 不改動原始物件：想加功能，不用回頭改原本的類別，也不用開一個子類別去繼承
2. 一層包一層：裝飾器把原本的物件包起來，自己也實作跟原物件相同的介面，呼叫端分不出來現在拿到的是本尊還是被包過的版本
3. 可以任意順序疊加：想加幾層就加幾層，想先加 A 再加 B，或先加 B 再加 A，結果都能成立

先舉個生活化的例子，包禮物

一份禮物本身就能送人，但想讓它更體面，可以先包上包裝紙，也可以再綁上緞帶，包裝紙、緞帶都不會動到禮物本身，只是在外面多包一層，而且想加幾層都行

```csharp
// 呼叫端在乎的介面：能描述自己、能算出價錢
public interface IGift
{
    string Describe();
    int Price { get; }
}

// 本體：一份最單純的禮物
public class BasicGift : IGift
{
    public string Describe() => "一份禮物";
    public int Price => 200;
}

// 不能 new 的抽象骨架（abstract class），嚴格遵守禮物的合約（實作 IGift）
public abstract class GiftDecorator : IGift
{
    protected readonly IGift _gift;

    protected GiftDecorator(IGift gift)
    {
        _gift = gift;
    }

    // 預設行為：直接轉交給被包裝的對象
    public virtual string Describe() => _gift.Describe();
    public virtual int Price => _gift.Price;
}

// 具體裝飾器：各自只負責「加一件事」，其他都轉呼叫被包住的物件
public class WrappingPaperDecorator : GiftDecorator
{
    public WrappingPaperDecorator(IGift gift) : base(gift) { }

    public override string Describe() => $"{_gift.Describe()}，包上包裝紙";
    public override int Price => _gift.Price + 30;
}

public class RibbonDecorator : GiftDecorator
{
    public RibbonDecorator(IGift gift) : base(gift) { }

    public override string Describe() => $"{_gift.Describe()}，綁上緞帶";
    public override int Price => _gift.Price + 20;
}
```

呼叫端這樣用

```csharp
IGift gift = new RibbonDecorator(new WrappingPaperDecorator(new BasicGift()));

Console.WriteLine($"{gift.Describe()}，{gift.Price} 元");
// 一份禮物，包上包裝紙，綁上緞帶，250 元
```

`BasicGift` 從頭到尾沒被改過，`WrappingPaperDecorator`、`RibbonDecorator` 也各自只知道自己要加什麼，包了幾層、包的順序，都是呼叫端當下決定的

這種感覺有點像是俄羅斯娃娃的概念

- 最內層核心：BasicGift（實作了 IGift）
- 中間層外殼：WrappingPaperDecorator（也是 IGift）
- 最外層外殼：RibbonDecorator（也是 IGift）

```text
呼叫端 (Client)
  │ 1. 呼叫 Describe()
  ▼
[ RibbonDecorator ]
    │
    │ 2. 向內問: _gift.Describe()
    ▼
[ WrappingPaperDecorator ]
      │
      │ 3. 向內問: _gift.Describe()
      ▼
[ BasicGift ]
        │
        │ 4. 回傳 "一份禮物"
        ▼
[ WrappingPaperDecorator ]
      │
      │ 5. 加工並回傳: "一份禮物，包上包裝紙"
      ▼
[ RibbonDecorator ]
    │
    │ 6. 加工並回傳
    ▼
string result = gift.Describe();  //一份禮物，包上包裝紙，綁上緞帶"
```

我也花了好久才能慢慢悟性其中的道理，回到 Decorator 的本質

1. 不改動原始物件: 沒有動到原始的 BasicGift
2. 對外部自由疊加功能功能（外部）：新增了包裝紙、新增了綁緞帶
3. 身分一致：外殼跟本體同一個介面，呼叫端分不出來現在拿到的是本尊還是被包過的版本

解決的核心重點: 如何不必寫死一堆子類別、也不必冒險修改舊程式碼破壞穩定性，就能動態且自由地為物件增減額外功能

### 回到柴咖啡

小黑最近在跟 Uber Eats、foodpanda 談合作，對方業務窗口丟來一份上架檢查清單，其中一條寫著:「商家須具備開立電子發票能力，每筆訂單完成都要能開」

小黑愣住了:「我們現在結帳都是手開收據耶，這個要怎麼弄？」

阿柴心裡盤算，這剛好順便解決另一件吧檯抱怨很久的事——尖峰時段吧檯要一邊聽單一邊手抄明細，常常抄錯品項、算錯金額，如果扣款成功後系統能自動印一張明細單，吧檯就不用再靠自己記

所以這次要處理的，是「扣款成功後」該自動做的兩件事

1. 開立電子發票(外送平台上架的硬性要求)
2. 印一張明細單給吧檯(減少手抄出錯)

阿柴翻開小黃留下的付款程式碼

```csharp
public class PaymentResult
{
    public bool Success { get; set; }
    public string TransactionId { get; set; }
}

public class PaymentService
{
    public PaymentResult Charge(string orderId, int amount)
    {
        // 呼叫金流商 API 進行扣款...
        Console.WriteLine($"訂單 {orderId} 扣款 {amount} 元成功");
        return new PaymentResult { Success = true, TransactionId = Guid.NewGuid().ToString() };
    }
}
```

## 先看看阿柴趕工出來的版本

時間也不多，阿柴先求有，兩個動作都直接寫進 `Charge()` 裡

```csharp
public class PaymentService
{
    public PaymentResult Charge(string orderId, int amount)
    {
        Console.WriteLine($"訂單 {orderId} 扣款 {amount} 元成功");

        var invoiceNumber = $"INV-{DateTime.Now:yyyyMMdd}-{orderId}";
        Console.WriteLine($"已開立發票：{invoiceNumber}");

        Console.WriteLine($"[明細單] 訂單 {orderId}，發票號碼：{invoiceNumber}");

        return new PaymentResult { Success = true, TransactionId = Guid.NewGuid().ToString(), InvoiceNumber = invoiceNumber };
    }
}
```

上線前勉強能動，兩件事都做了，但阿柴自己看了都皺眉

第一，開發票這段邏輯，想單獨測試「發票號碼格式對不對」，卻沒辦法脫離扣款、印明細單，只能整支方法一起測

第二，「明細單要印發票號碼」這個依賴，其實只是藏在方法裡的**執行順序**——`invoiceNumber` 要先算出來，印明細單那一行才印得出來。哪天有人在維護時，順手把兩段程式碼的順序調換，或在中間插入了新邏輯，程式碼一樣能編譯、能執行，但明細單就悄悄印出空白發票號碼，沒有任何錯誤訊息提醒

小黑這時候也丟了下一步計畫:「之後應該還要加一個，扣款完自動寄一封 Email 收據給客人」

阿柴心想，照現在的寫法，以後每加一個動作，都要回頭在 `Charge()` 裡面插隊，還得考慮插隊的位置跟其他動作的先後順序，這支方法只會越改越擠、越改越危險

問題不是阿柴手腳不夠快，而是**「扣款」「開發票」「印明細單」三件事的邏輯全部黏在同一個方法裡，彼此的執行順序被寫死在程式碼的先後位置上，看不出來、也測不出來誰依賴誰**

## Decorator 上場

阿柴先確認呼叫端真正在乎的介面，只有付款本身：傳訂單資訊，拿到 `PaymentResult`，不用改

```csharp
public interface IPaymentProcessor
{
    PaymentResult Charge(string orderId, int amount);
}

public class StandardPaymentProcessor : IPaymentProcessor
{
    public PaymentResult Charge(string orderId, int amount)
    {
        Console.WriteLine($"訂單 {orderId} 扣款 {amount} 元成功");
        return new PaymentResult { Success = true, TransactionId = Guid.NewGuid().ToString() };
    }
}
```

`PaymentResult` 多留一個欄位，讓「開發票」這個動作可以把發票號碼記回去，之後其他裝飾器才拿得到

```csharp
public class PaymentResult
{
    public bool Success { get; set; }
    public string TransactionId { get; set; }
    public string InvoiceNumber { get; set; } // 開立發票後才會有值
}
```

接著加一層裝飾器基底，長得跟 `IPaymentProcessor` 一樣，但內部包著另一個 `IPaymentProcessor`

```csharp
public abstract class PaymentDecorator : IPaymentProcessor
{
    protected readonly IPaymentProcessor _processor;

    protected PaymentDecorator(IPaymentProcessor processor)
    {
        _processor = processor;
    }

    public virtual PaymentResult Charge(string orderId, int amount) => _processor.Charge(orderId, amount);
}
```

「開發票」「印明細單」各自寫一個裝飾器，只管自己那件事，其他一律轉呼叫被包住的付款流程

```csharp
public class InvoiceDecorator : PaymentDecorator
{
    public InvoiceDecorator(IPaymentProcessor processor) : base(processor) { }

    public override PaymentResult Charge(string orderId, int amount)
    {
        var result = _processor.Charge(orderId, amount);
        if (result.Success)
        {
            result.InvoiceNumber = $"INV-{DateTime.Now:yyyyMMdd}-{orderId}";
            Console.WriteLine($"已開立發票：{result.InvoiceNumber}");
        }
        return result;
    }
}

public class ReceiptPrintDecorator : PaymentDecorator
{
    public ReceiptPrintDecorator(IPaymentProcessor processor) : base(processor) { }

    public override PaymentResult Charge(string orderId, int amount)
    {
        var result = _processor.Charge(orderId, amount);
        if (result.Success)
        {
            Console.WriteLine($"[明細單] 訂單 {orderId}，發票號碼：{result.InvoiceNumber ?? "（無）"}");
        }
        return result;
    }
}
```

呼叫端這樣用，付款流程本身完全沒被改動，只是在外面依序包了兩層

```csharp
// 開發票、印明細單，依序疊上去
IPaymentProcessor paymentFlow = new ReceiptPrintDecorator(new InvoiceDecorator(new StandardPaymentProcessor()));
paymentFlow.Charge("A001", 150);
// 已開立發票：INV-20260903-A001
// [明細單] 訂單 A001，發票號碼：INV-20260903-A001
```

而且現在 `InvoiceDecorator` 自己就是一個獨立的類別，想單獨驗證「發票號碼格式對不對」，包一個假的 `IPaymentProcessor` 進去測試就好，不用每次都連著扣款、印明細單一起測

小黑之後真的提了「扣款完寄 Email 收據」，阿柴只要新增一個 `EmailReceiptDecorator`，包在最外層或最內層都行，其他既有的裝飾器、`StandardPaymentProcessor` 完全不用動

## Decorator 的取捨：疊加順序，會影響結果

前面提到 Decorator「想先加 A 再加 B，或先加 B 再加 A，結果都能成立」，那句話說的是語法上兩種寫法都合法、都能正常執行，不代表語意上的結果一定一樣

阿柴一開始也以為兩個裝飾器誰包誰外面都沒差，直到他手滑把順序寫反

```csharp
// 順序顛倒：印明細單包在最外層，但發票還沒開出來
IPaymentProcessor wrongFlow = new InvoiceDecorator(new ReceiptPrintDecorator(new StandardPaymentProcessor()));
wrongFlow.Charge("A002", 150);
// [明細單] 訂單 A002，發票號碼：（無）　←　這時候還沒輪到 InvoiceDecorator 開發票
// 已開立發票：INV-20260903-A002　←　發票在明細單印完之後才開出來，太遲了
```

`ReceiptPrintDecorator` 包在裡面，會先執行；`InvoiceDecorator` 包在外面，反而後執行，印明細單那一刻，發票號碼根本還沒被寫進 `PaymentResult`

這是 Decorator 疊加時容易忽略的一點：**每個裝飾器只管自己要做的事，但疊的順序決定了「誰先誰後」，如果裝飾器之間有先後依賴（這裡是明細單依賴發票號碼），順序排錯，語法一樣合法，但結果是錯的**

**用組合，不是繼承**：裝飾器是在建構子把原本的物件包進來（組合），不是去繼承 `StandardPaymentProcessor` 再開一個 `PaymentWithInvoiceAndReceipt` 子類別（繼承）——這正是 Decorator 存在的理由，把「繼承會爆炸的組合數」換成「組合可以疊加的彈性」

**跟 Builder（Day 11）處理的層次不一樣**：Builder 是「把一個物件內部的欄位一步步組出來」，組完 `Build()` 之後形狀就固定了；Decorator 是「在一個已經存在的流程外面一層層加行為」，以後要加新動作，包哪幾層是呼叫端當下決定的，不用回頭改既有的流程

## 邊界在哪？什麼時候不需要 Decorator

如果柴咖啡「扣款後要做的事」永遠只有開發票這一件，不會再增加，那直接在 `Charge()` 裡面內建這個動作就好，不需要為此拉一整層 `PaymentDecorator` 階層

Decorator 真正該出手的時機，是**扣款後的收尾動作不只一種、彼此之間有先後依賴、各自的邏輯需要能獨立驗證、而且已經知道以後還會再加**——柴咖啡現在的開發票、印明細單，剛好符合這幾個條件

## AI 判斷力：這篇學到的判斷條件，能拿來檢查 AI 產出什麼

如果阿柴把「幫我在扣款成功後開發票、印明細單」丟給 AI，常見會看到兩種產出

一種是 AI 直接把兩段邏輯疊進 `Charge()` 方法裡——能動，但這正是阿柴踩過的坑，扣款、開發票、印明細單全部黏在一起，沒辦法單獨測試，順序依賴也只能靠人肉盯著程式碼順序，不容易一眼看出來

另一種是 AI 直接生出一整套「支付後處理管線（Post-Payment Pipeline）」框架，支援動態註冊處理步驟、設定檔控制執行順序、還留了外掛機制讓未來可以插隊——如果柴咖啡現在就只有開發票、印明細單兩件事，這種規格已經是設計過頭

AI 給得出「Decorator 該長什麼樣子」的語法，但它不知道柴咖啡現在有哪些收尾動作、動作之間有沒有像「明細單要印發票號碼」這種先後依賴、以後真的還會不會再加——這些屬於「這間店的收尾流程現在長怎樣」的判斷，答案不在程式碼裡，只有阿柴自己清楚

把這些條件講清楚告訴 AI:「目前扣款成功後固定要開發票、印明細單，明細單需要顯示發票號碼，所以要包在發票開完之後，之後預計還會加 Email 收據」，AI 疊出來的 Decorator 順序才會是對的，而不是語法正確、卻悄悄漏印發票號碼

---

明天，小黑正式要跟 Uber Eats、foodpanda 談的外送合作要上線了，柴咖啡內部的訂單格式跟這些平台傳來的資料完全對不上，這種「介面不相容，硬要搭起來合作」的情境，就換 **Adapter** 轉接器上場

## 參考資料

[Refactoring Guru - Decorator](https://refactoring.guru/design-patterns/decorator)

[[ Day 13 ] 你今天要來一份洋蔥圈嗎? - 裝飾者模式 ( Decorator Pattern )](https://ithelp.ithome.com.tw/articles/10206110)
