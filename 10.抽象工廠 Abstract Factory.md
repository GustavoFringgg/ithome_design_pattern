# Day 10｜Abstract Factory——早餐套餐與下午茶套餐,一次生成整組組合

昨天用 Factory Method 把「一種飲料怎麼生出來」交給子類別決定,阿柴總算不用再對著一大包 `if-else` 冒冷汗

今天小黑丟了一個新點子過來:「想推套餐,早上一套,下午茶一套,吸引不同時段的客人」

阿柴一聽,心想這不就是「飲料+點心」湊一湊而已嗎,應該很簡單

## 先看套餐出了什麼問題

柴咖啡的套餐其實分兩條線,彼此不能混搭

早餐套餐:美式/拿鐵 + 貝果 + 續杯券<br>
下午茶套餐:摩卡/拿鐵 + 蛋糕 + 甜點折價券

阿柴照小黃的老習慣,寫了兩個各自獨立的方法,分別決定飲料跟點心

```csharp
public class ComboService
{
    public IDrink GetDrink(string comboType)
    {
        if (comboType == "breakfast") return new Americano();
        else return new Mocha();
    }

    public ISideItem GetSide(string comboType)
    {
        if (comboType == "breakfast") return new Bagel();
        else return new Cake();
    }

    public IBonus GetBonus(string comboType)
    {
        if (comboType == "breakfast") return new RefillCoupon();
        else return new DessertDiscountCoupon();
    }
}
```

三個方法各司其職,看起來也符合「集中管理」的精神

但問題在於,這三個方法是**各自獨立**判斷 `comboType`,沒有人保證它們永遠同進退

店員操作收銀機,點了「早餐套餐」的飲料(美式),點心卻手滑點成「下午茶套餐」限定的蛋糕——這個組合在套餐價格表裡根本不存在,因為早餐套餐從來沒開放過「美式+蛋糕」這個搭配

結果收銀系統要嘛套不到套餐折扣,只能拆開單點,套餐優惠整個沒了；要嘛更糟,系統硬套了某個套餐價,卻附送了「甜點折價券」給一杯美式配貝果的客人,兌換邏輯完全對不上

這不是語法錯誤,三個方法各自看都沒問題,錯的是「早餐套餐的飲料、點心、贈品,是綁在一起的一整組,不該讓呼叫端自己兜」

## Abstract Factory 上場

工廠方法解決的是「一個產品怎麼生」,而阿柴現在遇到的是「一整組互相搭配、不能混用的產品,怎麼保證一次生對」,這正是 Abstract Factory 要處理的問題

**提供一個介面,用來建立一系列相關或相依的物件,而不需要指定它們的具體類別**

作法是把「早餐套餐」跟「下午茶套餐」各自包成一個工廠,呼叫端只需要選對工廠,之後跟這個工廠要什麼品項,拿到的一定是同一個套餐裡的東西,不會兜錯

```csharp
// ==========================================
// 1. 產品家族（Abstract Products）
// ==========================================
// IDrink 沿用 Day 9 的 Latte、Americano、Mocha
public interface ISideItem { string Name { get; } }
public interface IBonus { string Description { get; } }

// 早餐套餐家族
public class Bagel : ISideItem { public string Name => "貝果"; }
public class RefillCoupon : IBonus { public string Description => "本日續杯券"; }

// 下午茶套餐家族
public class Cake : ISideItem { public string Name => "蛋糕"; }
public class DessertDiscountCoupon : IBonus { public string Description => "甜點折價券"; }

// ==========================================
// 2. 抽象工廠（Abstract Factory）
// ==========================================
public interface IComboFactory
{
    IDrink CreateDrink();
    ISideItem CreateSide();
    IBonus CreateBonus();
}

// ==========================================
// 3. 具體工廠（Concrete Factories）：每個工廠只生產同一個套餐裡的品項
// ==========================================
public class BreakfastComboFactory : IComboFactory
{
    public IDrink CreateDrink() => new Americano();
    public ISideItem CreateSide() => new Bagel();
    public IBonus CreateBonus() => new RefillCoupon();
}

public class AfternoonTeaComboFactory : IComboFactory
{
    public IDrink CreateDrink() => new Mocha();
    public ISideItem CreateSide() => new Cake();
    public IBonus CreateBonus() => new DessertDiscountCoupon();
}
```

呼叫端長這樣

```csharp
public class ComboService
{
    // 只依賴抽象工廠介面，不知道也不需要知道現在是哪一套具體品項
    public void ServeCombo(IComboFactory factory)
    {
        IDrink drink = factory.CreateDrink();
        ISideItem side = factory.CreateSide();
        IBonus bonus = factory.CreateBonus();

        Console.WriteLine($"套餐出餐：{drink.Name} + {side.Name}，附贈 {bonus.Description}");
    }
}

public class Program
{
    public static void Main()
    {
        var comboService = new ComboService();

        // 早餐套餐：只要選對 BreakfastComboFactory，三樣品項自動成套，不用擔心兜錯
        comboService.ServeCombo(new BreakfastComboFactory());

        // 下午茶套餐：換一顆工廠，整組品項跟著換
        comboService.ServeCombo(new AfternoonTeaComboFactory());
    }
}
```

現在店員(或前台程式)只需要決定「這是早餐套餐還是下午茶套餐」一次,選對工廠之後,飲料、點心、贈品就會自動配成同一套,不會再有美式配蛋糕這種對不上套餐價格表的組合

## 跟 Factory Method 差在哪

阿柴一開始也搞混,兩個看起來都是「介面 + 具體工廠」,差異在哪?

Factory Method 解決的是**一個產品**怎麼生,重點是讓子類別決定「造出哪一個具體類別」,像昨天的 `LatteStore`、`AmericanoStore`,一家工廠只認一種飲料

Abstract Factory 解決的是**一整組相關產品**怎麼一起生對,重點是保證同一個工廠生出來的東西彼此搭配,不會混到別的家族去,`BreakfastComboFactory` 內部其實就是靠三個工廠方法(`CreateDrink`、`CreateSide`、`CreateBonus`)組成的

也就是說 Abstract Factory 常常是「疊在 Factory Method 之上的一層」,管的是產品之間的**成套關係**,而不是單一產品怎麼造

## 這個模式的代價

Abstract Factory 不是沒有成本,阿柴發現,如果哪天小黑想加一項新品項,例如所有套餐都要加「一張抽獎券」,這件事會牽動到整條線

`IComboFactory` 介面要加一個 `CreateLotteryTicket()`,所有實作它的具體工廠(`BreakfastComboFactory`、`AfternoonTeaComboFactory`……以後可能還有假日限定套餐工廠)都得跟著改

新增「一整組新套餐」(比如之後出了「宵夜套餐」)很輕鬆,直接多寫一個 `LateNightComboFactory` 就好,呼叫端完全不用動；但新增「套餐裡的一種新品項」,反而要動到介面跟每一個實作,這是 Abstract Factory 的典型取捨——換來成套的一致性,代價是**橫向擴充品項時,牽動範圍變大**

## 邊界在哪

不是每次看到「兩個 if-else 選規格」都要上 Abstract Factory,阿柴把判斷條件抓成兩個問題

第一,**這幾個產品之間,是不是真的有「不能混搭」的強制關係**?如果飲料、點心本來就可以自由組合,單點也是各自定價、互不影響,那分開幾個獨立的 Factory Method 就夠,硬包成 Abstract Factory 只是多繞一層

第二,**現在真的存在兩個以上的產品家族嗎**?如果柴咖啡目前只有早餐套餐一種,下午茶套餐還是小黑腦中的計畫,那先把 Abstract Factory 的骨架搭好,就是在為一個還沒發生的需求先付成本

## AI 產出程式碼時,Abstract Factory 能拿來檢查什麼

如果阿柴把「幫我寫早餐套餐跟下午茶套餐的出餐邏輯」丟給 AI,一樣容易看到兩種結果

保守版本,AI 生出跟小黃當年一樣的三個獨立 `Get` 方法,各自判斷 `comboType`,能動,但沒有任何機制保證三者永遠同進退,兜錯套餐的風險留在那裡沒被處理

過度發揮版本,AI 可能不管柴咖啡現在只有兩種套餐,就先包出 `IComboFactoryProvider`、`ComboFactoryRegistry`,甚至用反射動態載入工廠類別,這在只有兩種套餐、品項也不常變的情況下,是先幫一個還沒出現的第三、第四種套餐把路鋪好

這裡能拿來檢查 AI 產出的問題是兩層:**這幾個品項彼此之間真的不能混搭嗎**?如果答案是不一定、可以自由組合單點,那用不到 Abstract Factory；**現在真的有兩個以上的套餐在排隊嗎**?如果小黑只提了早餐套餐要上,下午茶套餐根本還沒定案,AI 卻已經先切好一整組可插拔的工廠註冊機制,那多半是它把「未來可能要加套餐」當成「現在就要支援任意數量的套餐家族」來設計了

AI 給得出 Abstract Factory 該怎麼組出這一整套介面跟工廠,但答不出「阿柴店裡的飲料跟點心,是不是真的綁死不能混搭」——這件事得靠人自己盯著套餐規則看,講清楚了,AI 給的架構才會剛好卡進柴咖啡現在的規模,不多也不少

---

明天客製化飲料要上場了,少冰、半糖、加燕麥奶,這種「同一杯飲料,但選項多到炸開」的組裝問題,工廠方法跟抽象工廠都不太夠用,該換 **Builder** 出馬了

## 參考資料

[Refactoring Guru - Abstract Factory](https://refactoring.guru/design-patterns/abstract-factory)
