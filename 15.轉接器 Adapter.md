# Day 15｜Adapter——串接 Uber Eats、foodpanda 資料格式轉換

創建型五天王告一段落，今天我們來進入結構型 Pattern（Structural）

第一個要來聊的是 Adapter 轉接器模式

我們來看看原文的定義，來自 Design Patterns: Elements of Reusable Object-Oriented Software

> Convert the interface of a class into another interface clients expect. Adapter lets classes work together that couldn't otherwise because of incompatible interfaces.

蝦咪意思，用中文理解可以是

> 把一個類別原本的介面，轉換成客戶端預期使用的另一種介面，讓原本因為介面不相容、沒辦法搭配使用的類別，可以互相合作

核心特性

1. 介面轉換：不改動雙方原始碼，額外加一層轉接層，把不相容的介面轉成呼叫端看得懂的樣子
2. 呼叫端無感：客戶端只認識自己熟悉的介面，不需要知道背後實際是哪個類別在做事
3. 常見於串接第三方 API、舊系統銜接新系統

先舉個生活化的例子，出國旅行時帶的「電源轉接頭」

台灣的家電插頭是 Type A，但歐洲插座是 Type C，兩者形狀不相容，吹風機插不進歐洲插座，這時候就需要一顆轉接頭

```csharp
// 呼叫端（吹風機）熟悉的介面：台灣插頭
public interface ITaiwanPlug
{
    void PlugIn();
}

public class TaiwanHairDryer : ITaiwanPlug
{
    public void PlugIn() => Console.WriteLine("吹風機插上台灣插座（Type A），開始運轉");
}

// 歐洲插座：介面完全不一樣，方法名稱也不同
public class EuropeSocket
{
    public void ConnectEU() => Console.WriteLine("接上歐規插座（Type C），開始供電");
}

// 轉接頭：對外實作吹風機認得的介面，內部實際去呼叫歐洲插座
public class EuropePlugAdapter : ITaiwanPlug
{
    private readonly EuropeSocket _europeSocket;

    public EuropePlugAdapter(EuropeSocket europeSocket)
    {
        _europeSocket = europeSocket;
    }

    public void PlugIn()
    {
        // 把「台灣規格的呼叫」轉換成「歐規插座聽得懂的呼叫」
        _europeSocket.ConnectEU();
    }
}
```

呼叫端完全不用管插座規格

```csharp
ITaiwanPlug dryerAtHome = new TaiwanHairDryer();
dryerAtHome.PlugIn(); // 吹風機插上台灣插座（Type A），開始運轉

ITaiwanPlug dryerInEU = new EuropePlugAdapter(new EuropeSocket());
dryerInEU.PlugIn(); // 接上歐規插座（Type C），開始供電
```

吹風機（呼叫端）從頭到尾只呼叫熟悉的 `PlugIn()`，轉接頭負責把訊號轉成插座聽得懂的格式，兩邊都不用被迫改自己的介面去遷就對方

## 回到柴咖啡

柴咖啡開始展店，小黑也談成了新生意：跟 Uber Eats、foodpanda 合作上架，開始接外送訂單

阿柴內部的訂單處理邏輯，一直以來只認得自己定義的 `Order`

```csharp
public class Order
{
    public string OrderId { get; set; }
    public List<OrderItem> Items { get; set; }
    public int TotalPrice { get; set; } // 內部一律用「元」為單位
}

public class OrderItem
{
    public string DrinkName { get; set; }
    public int Quantity { get; set; }
}

// 廚房只認得自己內部的 Order，不管訂單是從哪裡來的
public class KitchenService
{
    public void ReceiveOrder(Order order)
    {
        Console.WriteLine($"廚房收到訂單 {order.OrderId}，共 {order.Items.Count} 項，總金額 {order.TotalPrice} 元");
        // 這裡會扣庫存、印出單、通知製作
    }
}
```

但 Uber Eats、foodpanda 傳過來的訂單資料格式，長得完全不一樣

```csharp
// Uber Eats 傳來的格式
public class UberEatsOrderPayload
{
    public string ue_order_id { get; set; }
    public List<UberEatsProduct> products { get; set; }
    public int total_cents { get; set; } // Uber Eats 用「分」為單位
}

public class UberEatsProduct
{
    public string title { get; set; }
    public int qty { get; set; }
}

// foodpanda 傳來的格式
public class FoodpandaOrderPayload
{
    public string order_no { get; set; }
    public List<FoodpandaItem> items { get; set; }
    public decimal amount_twd { get; set; } // foodpanda 用「元」，但型別是 decimal
}

public class FoodpandaItem
{
    public string name { get; set; }
    public int count { get; set; }
}
```

## 先看看阿柴趕工出來的版本

離上架時間很近，阿柴先求有，直接在接單的 Controller 裡面，用 if-else 依平台各自解析

```csharp
public class OrderController
{
    public void HandleIncomingOrder(string platform, object payload)
    {
        Order order;

        if (platform == "ubereats")
        {
            var uePayload = (UberEatsOrderPayload)payload;
            order = new Order
            {
                OrderId = uePayload.ue_order_id,
                Items = uePayload.products
                    .Select(p => new OrderItem { DrinkName = p.title, Quantity = p.qty })
                    .ToList(),
                TotalPrice = uePayload.total_cents / 100 // 分轉元，得自己記得除
            };
        }
        else if (platform == "foodpanda")
        {
            var fpPayload = (FoodpandaOrderPayload)payload;
            order = new Order
            {
                OrderId = fpPayload.order_no,
                Items = fpPayload.items
                    .Select(i => new OrderItem { DrinkName = i.name, Quantity = i.count })
                    .ToList(),
                TotalPrice = (int)fpPayload.amount_twd
            };
        }
        else
        {
            throw new Exception("不支援的平台");
        }

        new KitchenService().ReceiveOrder(order);
    }
}
```

上線前能動，但阿柴後來自己看了都皺眉：格式轉換邏輯跟「收單後要通知廚房」這件業務邏輯，全部黏在同一支 `HandleIncomingOrder` 裡

某天 foodpanda 改版，把欄位從 `amount_twd` 改成 `amount`，阿柴要進去改這段判斷式時，手滑動到旁邊 Uber Eats 那個分支，順手改壞了縮排，結果 Uber Eats 的訂單也跟著炸掉——問題不是阿柴不小心，而是**兩個平台互不相干的轉換邏輯，被迫擠在同一支程式碼裡**，改一個會怕動到另一個

而且下個月小黑又談成了第三家外送平台，這代表 `HandleIncomingOrder` 又要多一個 `else if`，程式碼只會越疊越高

## Adapter 上場

阿柴先定義好呼叫端（柴咖啡內部系統）真正在乎的介面，只有一個目的：把外部資料轉成內部認得的 `Order`

```csharp
public interface IOrderSource
{
    Order ToOrder();
}
```

針對每個平台，各自寫一個轉接器，只做「格式轉換」這一件事

```csharp
public class UberEatsOrderAdapter : IOrderSource
{
    private readonly UberEatsOrderPayload _payload;

    public UberEatsOrderAdapter(UberEatsOrderPayload payload)
    {
        _payload = payload;
    }

    public Order ToOrder()
    {
        return new Order
        {
            OrderId = _payload.ue_order_id,
            Items = _payload.products
                .Select(p => new OrderItem { DrinkName = p.title, Quantity = p.qty })
                .ToList(),
            TotalPrice = _payload.total_cents / 100
        };
    }
}

public class FoodpandaOrderAdapter : IOrderSource
{
    private readonly FoodpandaOrderPayload _payload;

    public FoodpandaOrderAdapter(FoodpandaOrderPayload payload)
    {
        _payload = payload;
    }

    public Order ToOrder()
    {
        return new Order
        {
            OrderId = _payload.order_no,
            Items = _payload.items
                .Select(i => new OrderItem { DrinkName = i.name, Quantity = i.count })
                .ToList(),
            TotalPrice = (int)_payload.amount_twd
        };
    }
}
```

Controller 也跟著瘦身，不再需要知道任何平台細節

```csharp
public class OrderController
{
    public void HandleIncomingOrder(IOrderSource source)
    {
        Order order = source.ToOrder(); // 不管背後是哪個平台，只認得 IOrderSource
        new KitchenService().ReceiveOrder(order);
    }
}
```

呼叫端這樣用

```csharp
var controller = new OrderController();

controller.HandleIncomingOrder(new UberEatsOrderAdapter(uberEatsPayload));
controller.HandleIncomingOrder(new FoodpandaOrderAdapter(foodpandaPayload));
```

現在 foodpanda 改版欄位名稱，阿柴只要動 `FoodpandaOrderAdapter` 這一個類別，`OrderController`、`KitchenService`、Uber Eats 那條線完全不會被牽動；下個月真的要串第三家平台，也只是新增一個 `XxxOrderAdapter`，不用回頭改任何既有程式碼

## Adapter 的取捨

**Adapter 只做格式轉換，不要把業務邏輯塞進去**：不能在 `UberEatsOrderAdapter.ToOrder()` 裡面順手扣庫存或發通知，那是 `KitchenService` 的責任。Adapter 一旦身兼轉換又管業務邏輯，以後平台格式一改版，連業務邏輯都要跟著重新測試一次

**單位、精度這類細節，要收斂在 Adapter 內部處理乾淨**：像 `total_cents / 100` 這種換算，一定要包在 Adapter 裡面完成，不能讓 `KitchenService` 還要自己記得「Uber Eats 傳來的是分，得手動除以 100」，不然這種隱性規則遲早會被漏掉一次

**用組合，不要硬凹繼承**：C# 沒有多重繼承，上面的 Adapter 都是用組合（建構子把原始物件包進來）去實作，而不是嘗試去繼承第三方類別再想辦法改介面，這也是實務上比較常見的做法

## 邊界在哪？什麼時候不需要 Adapter

如果柴咖啡現在只串一家外送平台，短期內也沒有計畫再串第二家，格式也很穩定不常改版，直接在收到 payload 的地方寫一個轉換函式就好，不必要為了「以後可能會擴充」先建一整套 `IOrderSource` 介面加上多個 Adapter 類別

Adapter 真正該出手的時機，是**呼叫端的介面必須保持穩定，但背後串接的對象會變、會增加、格式也不受自己控制**——外送平台的資料格式是對方公司說改就改，阿柴完全無法控制，這種「不受自己控制、又預期會變動」的外部依賴，才是最適合用一層 Adapter 隔開的地方

## AI 判斷力：這篇學到的判斷條件，能拿來檢查 AI 產出什麼

如果阿柴把「幫我把 Uber Eats 跟 foodpanda 的訂單串進系統」丟給 AI，常見會看到兩種產出

一種是 AI 直接在 Controller 裡把兩個平台的 if-else 解析邏輯全部寫好，能動，但轉換邏輯跟收單、通知廚房的業務邏輯全部黏在同一支程式裡，以後要再串第三個平台，或哪個平台改版，就是阿柴踩過的那個坑

另一種是 AI 直接生出一整套 `IOrderSource`、`OrderAdapterFactory`、`OrderAdapterRegistry`，甚至留了設定檔可以動態註冊平台——如果柴咖啡現在真的只有 Uber Eats、foodpanda 兩家，短期內也沒有要再串更多平台，這種規格已經是設計過頭

AI 給得出「Adapter 該長什麼樣子」的語法，但它不知道柴咖啡現在到底串了幾個平台、這些平台的格式多常變動、未來半年會不會再談下一家外送平台——這些屬於「這門生意現在走到哪一步」的判斷，答案不在程式碼裡，只有阿柴自己清楚

把這些條件講清楚告訴 AI：「目前只有兩個平台，格式偶爾會改版，但半年內不會再談新平台」，AI 給出的 Adapter 層才會剛好卡住這兩家外送平台的變動，而不是幫一個還沒出現的第三家平台預先搭好一整套機制

---

明天來聊飲料加購，加濃縮、加珍珠、加奶蓋，這種「不動原本飲料本身，一直疊加功能」的情境，輪到 **Decorator** 裝飾者模式上場

## 參考資料

[Refactoring Guru - Adapter](https://refactoring.guru/design-patterns/adapter)
